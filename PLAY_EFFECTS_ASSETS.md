# 플레이 화면 연출 목록과 필요한 에셋

게임 중에 뜨는 **문구·연출 29가지**를 전수 조사해 "지금 무엇으로 그려져 있는지"와
"그림이 있으면 무엇이 좋아지는지"를 정리했습니다.

## 한 줄 요약

> **판(액자·띠·알약)은 그림으로 다 갈았지만, 연출은 거의 전부 이모지와 CSS입니다.**
> 조각 62개 중 화면에 실제로 쓰이는 것은 **20개**뿐이고, **36개는 잘라만 놓고 놀고 있습니다.**
> 새로 그리기 전에 그 36개부터 붙이면 상당 부분이 해결됩니다(§3).

연출에 쓰이는 이모지: `🐑` `🐰` `🧜‍♀️` `🌰` `🍃` `🔥⬆️` `🟢` `🔵` `👈` `👉`

---

## 1. 연출 목록

`지금`칸 표기 — **이모지**: 시스템 이모지 그대로 / **CSS**: 글씨·도형만 / **그림**: 에셋 사용

### A. 화면 한가운데 큰 배너 (6)

| # | 연출 | 언제 뜨나 | 지금 | 문구 |
| --- | --- | --- | --- | --- |
| 1 | `SheepLoadedBanner` | 지난 턴 실용신양으로 예약한 뽑기가 터지는 순간 | 이모지+CSS | 🐑 예약된 카드 N장 뽑기! |
| 2 | `FestivalLoadedBanner` | 축제 랜덤 뽑기가 터지는 순간(매 턴 반복) | 이모지+CSS | 🌰 도토리 축제 효과! 랜덤 뽑기 N회! |
| 3 | `FestivalStartBanner` | 축제 진입 **딱 한 번** | 이모지+CSS | 🌰 도토리 축제 시작! / 이제부터 N턴마다 추가 랜덤 뽑기 M회! |
| 4 | `DecisiveHitBanner` | 체력이 즉시 승패에 닿는 순간 | 이모지+CSS | GAME OVER! / 🟢 승리! |
| 5 | `TurnAnnounceBanner` | 턴이 넘어갈 때 | 이모지+CSS | 내 차례 / 상대 차례 / 👈 왼쪽 팀 차례 |
| 6 | `MainComboBanner` | 실용신양 연쇄가 끝났을 때 | **꺼져 있음** | N COMBO! |

> ⚠️ 6번은 `SHOW_COMBO_BANNER = false`로 렌더만 막아 둔 상태입니다. 계산 로직은 살아
> 있어 **값 하나만 되돌리면 다시 켜집니다.** 배너 그림을 만든다면 같이 살릴지 정해야 합니다.

### B. 날아가기 · 터지기 (6)

| # | 연출 | 언제 | 지금 | 비고 |
| --- | --- | --- | --- | --- |
| 7 | `WoolBallLayer` | 실용신양 예약 뽑기 — 팀 자리에서 장소로 | **이모지 🐑** | 한 번에 여러 개가 연달아 날아감 |
| 8 | `AcornBallLayer` | 축제 랜덤 뽑기 — 같은 방식 | **이모지 🌰** | 7번과 완전히 같은 구조 |
| 9 | `RabbitFlightLayer` | 상표토끼 발동 — 토끼떼가 체력 쪽으로 | **이모지 🐰** | 크기가 제각각인 여러 마리 |
| 10 | `TigerClawLayer` | 특허랑이 발동 — 발톱자국 3줄 | CSS(SVG 폴리곤) | 화면 전체 비네트가 함께 깔림 |
| 11 | `FestivalStartBurstLayer` | 축제 시작 — 도토리가 사방으로 | **이모지 🌰** | 3번 배너와 동시에 |
| 12 | `CardFocusLayer` | 장소를 클릭한 순간 | CSS(링 3개) | 그림 없이도 자연스러움 |

### C. 자막 · 떠오르는 글씨 (5)

| # | 연출 | 언제 | 지금 | 문구 |
| --- | --- | --- | --- | --- |
| 13 | `CardCaptionLayer` (pair) | 짝이 맞아 정산될 때, 그 스택 위 | CSS | "○○ 페어!" |
| 14 | `CardCaptionLayer` (effect) | 행동 효과가 발동할 때, 보드 중앙 | CSS | 효과 문구(팀에 따라 초록/빨강/금색) |
| 15 | `EffectLayer` 플로팅 텍스트 | 경험치·체력이 오르내릴 때 | CSS | +N / −N |
| 16 | `EffectLayer` 나뭇잎 | 큰 일이 벌어질 때 화면 위에서 낙하 | **이모지 🍃** | 개수가 사건 크기에 비례 |
| 17 | `SheepComboLayer` | 연쇄 중 콤보 수 | **꺼져 있음** | N combo! |

### D. 상태 표시 (6)

| # | 연출 | 언제 | 지금 |
| --- | --- | --- | --- |
| 18 | `FestivalProgressBar` | 축제 추가 뽑기 진행도 | **그림 ✓** `acorn_bar` |
| 19 | `SheepProgressBar` | 실용신양 예약 뽑기 진행도 | CSS(검은 알약) — 18번과 짝인데 혼자 그림이 없음 |
| 20 | 레벨업 (`ScorePanel`) | 동물 레벨이 오를 때 | **이모지** 🔥⬆️ Lv UP! |
| 21 | 인어 배율 스티커 | 디자인어로 배율이 쌓였을 때 | CSS · "×N" |
| 22 | 예약 뽑기 스티커 | 실용신양 예약분이 남아 있을 때 | CSS |
| 23 | `MermaidPopup` | 디자인어 발동 | **이모지 🧜‍♀️** |

### E. 피격 · 반동 (4, 전부 CSS)

| # | 연출 | 언제 |
| --- | --- | --- |
| 24 | `panel-recoil-a/b` | 행동을 **발동한** 팀 판이 뒤로 젖혀짐 |
| 25 | `panel-hit-shake` | **맞는** 팀 판이 흔들림 |
| 26 | `panel-rabbit-pressure` | 토끼떼가 부딪힐 때의 압박 |
| 27 | `hp-orb-surge` / `crack` / `shard` | 체력 구슬이 차오르거나 금 가고 파편이 튐 |

> 이 넷은 **움직임**이라 그림이 필요 없습니다. 다만 27번 체력 구슬은 유리구슬 모양이
> 나무·양피지 세계관과 겉돕니다(로드맵 §8-10의 "체력판을 줄 노트로" 항목).

### F. 그 밖

| # | 연출 | 지금 |
| --- | --- | --- |
| 28 | `GameEndScreen` 승/패/무 | CSS + 동물별 축하 문구 |
| 29 | `GuideFinger` · `PlayerEmoticonLayer` · `DrawSlotLayer` | **그림 ✓** (손 4종, 표정 20종) |

---

## 2. 필요한 에셋

### 우선 1 — 이모지를 대체하는 토큰 (효과 가장 큼)

이모지는 **OS마다 모양이 달라** 보는 사람에 따라 화면이 달라지고, 그림체도 겉돕니다.
연출 7·8·9·11·16·20·23이 전부 여기 걸립니다.

| 에셋 | 쓰이는 곳 | 권장 규격 |
| --- | --- | --- |
| `token_sheep` 🐑 | 7번(예약 뽑기), 1번 배너 | 정사각 256², 투명, 외곽선 굵게 |
| `token_rabbit` 🐰 | 9번(토끼떼) | 〃 — 여러 마리가 겹치므로 실루엣이 또렷해야 함 |
| `token_mermaid` 🧜‍♀️ | 23번(인어 팝업) | 〃 |
| `token_tiger` 🐯 | (지금은 발톱만) | 〃 |
| `token_acorn` 🌰 | 8·11번 | **이미 `icon_acorn`이 있습니다** → 붙이기만 하면 됨 |
| `leaf_particle` 🍃 | 16번 낙하 파티클 | 128², 2~3종(각도 다르게)이면 더 자연스러움 |

> ⚠️ 토큰은 **작게 그려도 알아볼 수 있어야** 합니다. 날아가는 중에는 40~60px로 그려지고
> 여러 개가 겹칩니다. 얼굴 표정(`/emoticon`)을 그대로 쓰면 뭉개집니다 —
> **단순한 실루엣 + 굵은 외곽선**이 필요합니다.

### 우선 2 — 배너 바탕

연출 1~6이 전부 맨 글씨입니다. 배경이 숲 그림이라 글씨만으로는 대비가 약합니다.

| 에셋 | 용도 | 권장 규격 |
| --- | --- | --- |
| `banner_wide` | 1·2·5번 한 줄 배너 | 가로로 긴 나무 팻말 · **가로 3분할**(양 끝 마구리 + 늘어나는 가운데) |
| `banner_tall` | 3·4번 두 줄 배너 | 〃 · 세로가 두 줄 들어갈 만큼 |
| `banner_gold` | 4번 GAME OVER 전용 | 금테/붉은 톤 등 "결정적 순간"용 별색 |

> ⚠️ 글씨가 들어가므로 **반드시 가로 3분할**로 그려 주세요(좌 마구리 │ 늘어나는 가운데 │
> 우 마구리). 통짜로 늘리면 문구 길이에 따라 마구리가 찌그러집니다.
> 지금 쓰는 `bar_dark`·`acorn_bar`와 같은 방식입니다.

### 우선 3 — 나머지

| 에셋 | 용도 | 비고 |
| --- | --- | --- |
| `badge_levelup` | 20번 "Lv UP!" | **`badge_crown`이 이미 있습니다** — 대체 가능할지 먼저 확인 |
| `claw_mark` | 10번 발톱자국 | 지금 SVG. 그림으로 바꾸면 그림체가 통일됨(필수는 아님) |
| `sheep_progress_bar` | 19번 진행도 | 18번(`acorn_bar`)과 짝. **나무 띠 + 양 한 마리**면 대칭이 맞음 |
| `hp_note` | 27번 체력판 | 유리구슬 → 줄 노트. **`note_green_rows`·`note_pink_rows`가 이미 있습니다** |

---

## 3. ⚠️ 새로 그리기 전에 — 잘라 놓고 안 쓰는 조각 36개

조각 62개 중 **화면에 실제로 붙어 있는 것은 20개**입니다. 나머지는 경로만 선언돼 있습니다.

**바로 쓸 수 있는 것**

| 조각 | 붙일 자리 |
| --- | --- |
| `icon_acorn` | 8·11번 도토리 토큰 (**이미 헤더 배지에서 쓰고 있음** — 연출에도 재사용) |
| `icon_lock` | 장소 금지 표시(지금은 CSS 금지 마크) |
| `badge_crown` | 20번 레벨업 배지 · 28번 승리 화면 |
| `note_green_rows` / `note_pink_rows` | 27번 체력판(줄 노트 4칸 = 동물 4종과 칸 수가 맞음) |
| `gauge` / `gauge_track` / `gauge_fill` | 타이머(지금 track·fill만 씀, `gauge`는 모래시계까지 한 벌) |
| `arrow_green` / `arrow_wood` | 5번 턴 전환 배너의 방향 표시(👈👉 대체) |
| `pill_sm_*` 3종 · `pill_leaf` | 21·22번 스티커 |
| `decor_*` 7종 | 액자 바깥 귀퉁이 장식(로드맵 §8-10 미착수 항목) |

**당분간 쓸 일이 없는 것** — `board_h/sq/v`, `frame_wide/square/tall`, `paper_*`, `pg_*`,
`card_frame`, `card_plaque`, `bar_light`: 예전 구조(판마다 제 액자)에서 쓰던 것이라
통짜 액자로 바뀌며 역할이 사라졌습니다. **지우지는 않았습니다** — 되돌릴 여지가 있고,
양피지 색(`#fee9bb`)을 여기서 뽑았습니다.

---

## 4. 작업 순서 제안

1. **`icon_acorn`을 8·11번에 붙이기** — 새 그림 0장, 바로 효과가 보입니다.
2. **동물 토큰 4종 + 나뭇잎** — 이모지가 사라져 화면이 한 그림체로 묶입니다.
3. **배너 바탕 2~3종** — 중앙 연출 여섯 개가 한꺼번에 해결됩니다.
4. **체력판을 줄 노트로** — 세계관에서 가장 크게 겉도는 자리입니다.

> ⚠️ 새 조각을 받으면 `client/assets-src/`에 두고 `node scripts/cutPlaySheet.mjs`로 자릅니다.
> 3분할·9분할 값과 함정은 `client/public/play/README.md`에 정리돼 있습니다.

# 인수인계 — 로비 개편 (2026-09-08 기준, 그림 버튼·기본 글꼴·Render 배포까지)

> **환경을 새로 구성하는 중이라면 [`NEXT_SESSION.md`](NEXT_SESSION.md)를 먼저 보라.**
> 이 파일은 로비 개편의 **작업 이력과 기술적 함정**을 담는다. 환경 구성·전체 현황·
> 다음 할 일은 그쪽에 있다.

이 저장소(`Aringco/ip_card_battle_render`)에서 이어서 작업하면 된다.
이전에 쓰던 `ip_card_battle` 저장소·폴더는 더 이상 쓰지 않는다(§5 참조).

## 1. 지금 어디까지 되어 있나

`f378b86 feat: 로비 화면 개편 - 카드테이블 배경 + 슬라이드 전환 UI` 로 커밋·push 완료.

로비 화면이 **세로 버튼 3개 나열 → 카드테이블 배경 위 2단 선택 구조**로 바뀌었다.

```
home ──멀티──▶ multi ──방 만들기──▶ create ──▶ waiting ──▶ /room/[id]
 │               │                             ▲
 │               └──방 참가하기──▶ join ────────┘
 └───싱글────▶ solo ──────────────────────────────▶ /room/[id]
```

- `page.tsx` 477줄 → 193줄. 폼·대기실은 `client/components/lobby/` 11개 파일로 분리
- 전환은 전부 CSS — `data-stage` 속성 하나만 바꾸면 뒤로가기가 정확한 역재생이 된다
- 배경 일러스트 위 "안전영역"에 버튼이 놓여, 화면 비율이 어떻게 바뀌어도 캐릭터를 가리지 않는다

그 뒤로 §6(upstream 로비 기능 이식)·§7(폼 백보드)·§8(관전자)·§9(액자를 대기실·결과 창까지)·§10(메달·월계수)·§11(스크롤·장식 정리)·§12(폼 5:5 재배치·글자 되돌림)가
얹혔다. **지금 화면을 이해하려면 §6~§12까지 봐야 한다** — §1~§5는 그 이전 단계의 기록이다.

설계 배경·실측값·시행착오는 [`LOBBY_REDESIGN.md`](LOBBY_REDESIGN.md)에, 동작 원리 요약은
[`CLAUDE.md`](CLAUDE.md)의 "로비 화면" 절에 있다. **로비를 건드리기 전에 그 절을 먼저 읽을 것.**

## 2. 이 저장소 고유 기능은 그대로 살아 있다

`ip_card_battle`에서 옮겨 오면서 이 저장소에만 있던 것들을 모두 보존했다.

| 기능 | 어디에 |
| --- | --- |
| `LoadingScreen` 에셋 프리로드 게이트 | `page.tsx`의 `assetsReady` |
| 상대 팀 이름(`otherTeamName`) | `CreateRoomForm` → `createRoom` 5번째 인자 |
| 선 플레이어(`firstTeam`) 3지선다 | `GameRulesFields` |
| 글씨 크기 설정(`--font-scale`) | Tailwind `--text-*` 전역 재정의라 자동 적용. `.input-base`에도 연결해 둠 |
| 하단 설정 안내 문구 | `page.tsx` 하단 (어두운 펠트에 맞춰 흰 글씨) |

## 3. 실행 방법

```bash
npm install          # 반드시 루트에서 (워크스페이스 구조)

# 터미널 2개
cd server && npm run dev     # WebSocket, 8080
cd client && npm run dev     # Next.js, 3000
```

## 4. 로비를 수정할 때 지켜야 할 것

### 폼에 항목을 추가하면 반드시 다시 잴 것

폼(`.stage-form`)은 `position: absolute`라 스테이지 박스를 밀어낼 수 없다. 항목을 하나만
더해도 조용히 잘리거나 스크롤바가 생기는데 눈으로는 잘 안 보인다.

```bash
cd server && npm run dev                   # 터미널 1 — WS 8080 (아래 이유로 반드시 필요)
cd client && npx next build && npx next start -p 3200   # 터미널 2
node scripts/measureLobby.mjs              # 터미널 3 (client에서)
```

**WS 서버(8080)를 같이 띄워야 한다.** 안 띄우면 `page.tsx`가 연결 실패를 `blocked`로 보고
패널을 `disabled`로 만들어, 스크립트가 패널을 눌러도 폼이 열리지 않는다. 표의 "카드"·"폼스크롤"
칸이 전부 `-`로 나오면 정확히 이 상황이다 — 문제 없음으로 보이지만 실은 아무것도 재지 못한 것이다.

3개 뷰포트 × 7단계를 재고 `client/lobby-shots/`에 스크린샷을 남긴다.
**폼 스크롤·페이지 스크롤·화면 이탈이 모두 `no`여야 하고, "비고" 칸이 비어 있어야 한다**
(비고에 `***`가 뜨면 그 단계는 재려던 상태에 도달하지 못한 것이다).

> **기준값은 [`NEXT_SESSION.md`](NEXT_SESSION.md) §2-6 한 곳에만 둔다.**
> 예전에는 이 자리에도 표를 두었는데, 백보드 작업이 그쪽만 갱신하면서 두 문서가
> 서로 다른 값을 말하는 상태로 하루를 보냈다(여기 `358×517` vs 저기 `706×486`).
> 절차는 여기, 숫자는 저기 — 한쪽만 고쳐도 어긋나지 않게 갈라둔다.

> dev 서버로 재면 Turbopack이 낡은 CSS 청크를 내주는 경우가 있어 값이 코드와 안 맞을 수
> 있다(이번 작업에서 두 번 겪었다). 반드시 프로덕션 빌드로 잴 것.

### 배경 이미지를 바꾸면

`client/lib/lobbyAssets.ts`의 경로 한 줄만 바꾸면 된다. 구도가 다른 이미지라면
`globals.css`의 `--table-w/h`와 `--safe-*` 4개 값도 맞춘다
([`client/public/lobby/README.md`](client/public/lobby/README.md)에 좌표 의미가 적혀 있다).

### `public/`에 이미지를 추가하면

`client/scripts/generateAssetManifest.mjs`의 `IMAGE_DIRS`에 그 폴더가 있어야
`LoadingScreen`이 프리로드한다. 빠뜨리면 로딩 화면이 끝난 뒤에야 받아와 배경이 늦게 뜬다.
(이번에 `lobby`, `tmp`를 추가했다.)

## 5. 남은 일

### ~~패널 배경이 아직 임시 에셋이다~~ → 정리 완료 (2026-09-03)

임시 SVG 4종(`public/tmp/panel_*_bg.svg`)을 **CSS 그라디언트로 대체**했다. placeholder
표식(점선 테두리·해치 무늬)이 완성된 배경 위에서 "웬 점선 사각형"으로 보이던 문제가
사라졌고, 그림이 담고 있던 정보는 어차피 초록 그라디언트 한 줄뿐이었다.

- 색은 `globals.css`의 `.lobby-panel-{solo,multi,create,join}`이 `--panel-a/b`로 넘긴다
  (원본 SVG의 그라디언트 값을 그대로 옮겼다 — 싱글만 한 단계 밝은 것은 의도)
- `ModePanel`의 `bgSrc: string` prop이 `tone: PanelTone`으로 바뀌었다
- 확정 로고 `logo.png`는 `public/tmp/` → **`public/lobby/`** 로 옮겼고 `public/tmp/`는 삭제
- `generateAssetManifest.mjs`의 `IMAGE_DIRS`에서 `tmp`를 뺐다 (프리로드 이미지 41 → 37개)
- 아무 데서도 쓰이지 않던 죽은 상수 `TMP_ASSETS_IN_USE`도 함께 삭제

프로덕션 빌드로 §4의 실측을 다시 돌려 기준값이 그대로임을 확인했다(문제·콘솔 에러 없음).

### 패널 아트 4종 투입 (2026-09-03)

확정 일러스트가 네 패널 모두에 깔렸다. 톤 그라디언트는 이미지 도착 전 한 프레임의
바탕으로만 남았다.

- `public/lobby/panel_solo.webp` — 네 동물 vs 로봇(= CPU 대전)
- `public/lobby/panel_multi.webp` — 네 동물이 테이블에 둘러앉은 그림(= 팀 대전)
- `public/lobby/panel_create.webp` — 실용신양이 오두막을 짓는 그림(= 방장이 방을 만든다)
  (세 번 교체됐다 — 창문이 비어 있고 문이 열린 버전이 최종)
- `public/lobby/panel_join.webp` — 셋이 그 오두막 문을 두드리는 그림(= 남의 방에 들어간다)
  (한 번 교체됐다 — 조금 더 물러서서 넷이 다 보이는 버전이 최종)

두 그림은 **같은 오두막**이다 — 방 만들기는 짓는 쪽, 방 참가하기는 찾아오는 쪽.
어느 한쪽만 갈아끼우면 이 짝이 깨지니 함께 볼 것.
- 각 768×768 / 80~95KB. 원본 1024² JPEG를 `sharp`로 줄였다(로비 프리로드 대상이라 용량이
  곧 첫 진입 대기시간이다)
- 버튼 문구가 `싱글플레이`/`멀티플레이` → **`혼자 놀기`/`다같이 놀기`** 로 바뀌었다
- **이모지는 네 패널 모두에서 뺐다.** 그림이 이미 같은 것을 말하고 있고, 특히 싱글은
  이모지 로봇이 그림 속 로봇 얼굴 바로 위에 겹쳤다. `ModePanel`의 `emoji`는 선택 prop으로
  남겨뒀으니, 아트 없는 패널을 새로 만들면 그때 다시 쓰면 된다

**어느 그림이 어느 버튼인지 헷갈리기 쉽다.** 로봇이 나오는 그림이 *싱글*이다(컴퓨터와 대전).
동물 넷만 나오는 그림이 *멀티*(친구와 팀 대전). 처음 지정이 반대로 와서 한 번 바로잡았다.

**아트 패널은 톤 패널과 CSS 처리가 반대다.** 그라디언트는 반투명하게 깔아 테이블이 비치게
두면 됐지만, 그림을 반투명하게 만들면 펠트 무늬와 섞여 무엇을 그린 것인지 알아볼 수 없다.
그래서 `.lobby-panel-art`는 불투명하게 깔되 어둡게 눌러 두고(brightness 0.5 + blur 2px),
hover하면 흐림이 풀리며 제 색으로 밝아진다. 이때 주의할 것 두 가지:

- **`blur`에는 `scale(1.05)`가 딸려 있어야 한다.** `filter: blur()`는 요소 가장자리 바깥의
  투명까지 번지게 해서 패널 테두리에 희끄무레한 띠를 만든다. 살짝 키워 그 띠를 잘라낸다.
  `prefers-reduced-motion`에서도 `transform: none`이 아니라 **양쪽 상태를 같은 배율로 고정**
  해야 한다(none으로 되돌리면 띠가 다시 생긴다).
- **글자 뒤 어둠(`.lobby-panel-art .stage-panel-label`)은 타원이 아니라 가로 띠다.** 그림들이
  인물로 꽉 차 있어, 가운데를 원형으로 파내면 띠 밖으로 삐져나온 부제 끝이 시끄러운 그림
  위에 그대로 얹힌다.

또 하나 같이 고친 것: **멀티 단계에서 두 버튼을 감싸던 "액자"를 없앴다.** `.stage-panel-multi`는
조건부 렌더링을 하지 않는 구조라 create/join 단계에서도 계속 마운트돼 있는데, 테두리·그림자가
바깥 윤곽을 그리고 두 열 사이 간격으로 멀티 그림이 비쳐 얼룩진 띠가 됐다. `box-shadow: none` +
`border-color: transparent` + 배경 `opacity: 0`으로 숨긴다 — `border: none`으로 하면 1px이
사라지면서 안쪽 배치가 그만큼 밀리므로 `transparent`여야 한다.

**단, 지우는 시점이 중요하다.** 처음엔 단계가 바뀌는 즉시 지웠는데, 그러면 확장이 시작되기도
전에 방금 누른 패널이 증발해 넓어지는 빈 공간만 남는다. 그래서 `transition-delay: var(--t-expand)`
로 **확장이 끝날 때까지 기다렸다가**, `var(--t-reveal)` 길이로 안쪽 2열이 드러나는 것과 함께
사라진다. 이 지연·길이는 바로 아래 `.multi-inner`의 clip-path·opacity와 일부러 같은 값이다 —
한쪽만 바꾸면 교대가 어긋난다. 실제로 재보면 크로스페이드가 정확히 맞물린다:

| 경과 | 열 너비 | 멀티 패널 배경 | 안쪽 2열 |
| --- | --- | --- | --- |
| 0ms | 362 → | 1.00 | 0.00 |
| 300ms | 738 (확장 끝) | 1.00 | 0.00 |
| 393ms | 738 | 0.42 | 0.58 |
| 468ms | 738 | 0.11 | 0.89 |
| 560ms | 738 | 0.00 | 1.00 |

돌아올 때는 이 규칙이 통째로 떨어져 나가 지연이 사라지므로, 패널이 먼저 제 모습으로 돌아온 뒤
(0~200ms) 열이 줄어든다(200~600ms) — 정확한 역재생. 이때 테두리만 뚝 끊기지 않도록
`.lobby-panel`의 기본 transition에 `border-color`를 넣어뒀다.

### 버튼 테두리와 hover 글로우 (2026-09-03)

패널에 상시 흰 테두리(`2px` / `rgba(255,255,255,0.7)`)를 두고, hover하면 버튼 주변으로
흰빛이 살짝 번지게 했다. **이 로비에서 바깥으로 뻗는 그림자는 생각보다 까다롭다.**

- `.stage-col`은 슬라이드 아웃을 위해 `overflow: hidden`이고 패널은 열을 정확히 채운다.
  그래서 **패널에 건 바깥 box-shadow는 한 번도 그려지지 않는다.** `.stage-panel`에 있던
  `box-shadow: 0 6px 18px …`도 그런 죽은 선언이라 이번에 지웠고, hover 강조를 안쪽 링
  (`inset 0 0 0 3px`)으로 그리던 것도 같은 이유였다.
- 해법은 그림자를 **열**에 거는 것이다 — 열 자신의 box-shadow는 자기 overflow에 잘리지
  않는다. 열과 패널은 크기가 같으니 결과는 "버튼에서 빛이 퍼진다"와 똑같다.
  `:has(> …)`로 직계 자식만 보는 이유는 멀티 열 안에 방 만들기/참가하기 열이 중첩돼 있어,
  안쪽 버튼에 hover할 때 바깥 멀티 열까지 같이 빛나면 안 되기 때문이다.
- 그 **중첩된 두 열은 자르개가 두 겹**이다(`.stage-col-multi`의 overflow +
  `.multi-inner`의 clip-path). 그냥 두면 글로우가 열 사이 간격 쪽으로만 새어 나오고 나머지
  세 방향은 잘린다. 멀티 단계에 한해 두 겹을 `--glow-reach`(14px)만큼 연다
  (`overflow-clip-margin`, 음수 `inset()`).
- **바깥 멀티 열은 hover 중일 때만 열어야 한다.** 늘 열어두면 싱글을 골라 그 열이 폭 0으로
  줄 때 잘려야 할 패널 조각이 화면 오른쪽에 14px 띠로 남는다. overflow는 애니메이션
  대상이 아니라 hover에 맞춰 즉시 켜지고 꺼진다. 실제로 싱글 단계까지 들어가 띠가 남지
  않는 것을 확인했다.
- 뒤로가기 버튼(`.lobby-back`)의 원형 배지에도 같은 빛을 걸었다. **값은 일부러 다르다**
  (12px/0.65) — 지름 36px 배지에 패널과 같은 14px/0.45를 쓰면 번짐이 배지보다 커져
  형체 없는 얼룩이 된다. 배지 바깥 평균밝기로 재보면 78.8 → 109.7이다.
- 폭 변수 `--glow-reach`는 `.lobby-safe`에 있다 — 뒤로가기 버튼이 `.lobby-stage`의
  **형제**라, 스테이지에 두면 변수가 닿지 않는다.

### 패널 문구 위치는 그림이 정한다 (2026-09-03)

방 만들기·방 참가하기는 주인공(양 한 마리 / 셋)이 그림 **아래쪽**에 있어서, 라벨을
가운데에 두면 정확히 그들을 가린다. `.lobby-panel-create/join .stage-panel-label`에서
`justify-content: flex-start`로 위(초가지붕)로 올리고, 어둠 띠도 위로 옮겼다.
혼자·다같이 놀기는 인물이 위아래로 고루 퍼져 있어 가운데가 맞다.
**그림을 갈아끼울 때 이 대응이 여전히 맞는지 함께 확인할 것.**

### ~~`cd server && npm run build`가 실패한다~~ → 수정 완료 (2026-09-03)

서버 코드와 무관한 곳에서 터지던 것이라 원인이 잘 안 보였다.

```
../node_modules/@types/react-dom/index.d.ts(91,22): error TS2304: Cannot find name 'ReferrerPolicy'.
```

`server/tsconfig.json`이 `types`를 지정하지 않아 TypeScript가 `node_modules/@types/*`를 전부
자동으로 끌어왔고, 워크스페이스 루트에 함께 설치된 **클라이언트용 `@types/react-dom`** 이
DOM 전역을 요구하는데 이 서버의 `lib`에는 DOM이 없다. `"types": ["node", "jest"]`로 목록을
좁혀 해결했다(`lib`에 DOM을 더하는 방법도 있지만, 그러면 Node 서버 코드에서 `document`/`window`가
실수로 컴파일을 통과한다).

`npm run dev`(ts-node)와 `npm test`(ts-jest)는 멀쩡히 돌아갔다 — **`npm run build`에서만
드러나는 증상**이라 눈치채기 어렵다. 자세한 설명은 `CLAUDE.md`의 "개발 명령어" 절에 남겼다.

### 옛 저장소 정리

| 대상 | 상태 |
| --- | --- |
| 로컬 `ai_esports_contest/ip_card_battle/` | 백업 브랜치·태그가 남아 있어 참고용으로만 유용 |
| `Aringco/ip_card_battle` (GitHub fork) | 이제 안 씀 — 삭제할지 판단 필요 |
| `ai_esports_contest/lobby-patch-2026-09-03/` | 이식에 썼던 패치 35MB. 이식이 끝났으니 삭제 가능 |

> 옛 저장소(`ip_card_battle`)와 이 저장소는 **공통 git 이력이 전혀 없다.** 그래서 이번 이식도
> merge/rebase가 아니라 파일 단위 복사로 했다. 앞으로 두 저장소를 git으로 연결하려 하지 말 것.

## 6. upstream 병합 — 로비 기능 이식 (2026-09-04)

upstream/main 7개 커밋을 병합해 `origin/main`에 반영했다(`ede6b49..8b7d1b6`). 배경과 남은 판단은
[`NEXT_SESSION.md`](NEXT_SESSION.md) §5-0에 있고, 여기에는 **로비를 어떻게 건드렸는지**만 적는다.

두 갈래는 손댄 영역이 거의 겹치지 않았다(내 쪽=로비 화면, upstream=게임 로직+대기실).
서버 엔진·`shared`·게임 화면·`globals.css`의 게임 연출은 전부 자동 병합됐고 **충돌은
로비 3건뿐**이었다. 해결 방식은 §5-0의 표에 있다.

### 이식하면서 로비에 생긴 규칙 — 되돌리면 조용히 깨진다

**경고는 새 줄로 덧붙이지 말고 있는 줄을 갈아끼운다.** 팀 이름 충돌 경고를 별도 `<p>`로
붙였더니 실측에서 잡혔다 — 390×844에서 방 만들기 카드가 517 → **545px**가 되며 폼이
스크롤했다(1280·1920에서는 여유가 있어 통과했다. 좁은 화면만 걸린다).

해결은 `FormCard`의 `description`을 `ReactNode`로 받아 **평소 설명 줄을 경고로 교체**하는
것이다. 높이 증가가 구조적으로 0이 된다.

| 상태 | 390×844 카드 높이 |
| --- | --- |
| 방 만들기 + 규칙 | 345×517 |
| 방 만들기 + 규칙 + 경고(별도 `<p>`) | 345×545 → **폼 스크롤 발생** |
| 방 만들기 + 규칙 + 경고(설명 줄 교체) | 345×517 → 문제 없음 |

어느 칸이 문제인지는 `TeamNameField`의 `invalid`가 붉게 표시하는데, **반드시 `ring`이어야
한다** — `border-width`를 1px→2px로 키우면 입력창이 2px 높아져 같은 문제가 그대로 재현된다.

**대기실만 안전영역 밖으로 나간다.** upstream 대기실은 채팅·방장 조작·규칙 수정이 붙어
내 109줄짜리보다 훨씬 길다. 안전영역(1280×900에서 738×628, 그중 로고가 46%)에 그대로 넣으면
방 코드와 팀 목록 윗부분만 보이고 **시작 버튼이 스크롤 밖으로 밀린다**(실제로 그렇게 됐다).

안전영역은 *배경 위에 얹히는 반투명 UI*가 캐릭터를 가리지 않게 하려는 장치인데, 대기실은
불투명한 흰 카드 하나라 그 제약이 애초에 무의미하다. 그래서
`.lobby-safe[data-stage="waiting"]`에서만 좌우/상하 제약을 풀고 세로로 꽉 쓰게 했다(가로는
카드 자신의 `max-w-4xl`에 맞춘다). 로고 슬롯은 폼이 열릴 때와 **같은 규칙**으로 함께 접힌다.
`data-stage="waiting"`은 로비 개편 때 이미 붙여두고 쓰지 않던 속성이라, 훅은 그대로 쓰였다.

> 이건 "쓸 수 있게" 만든 것이지 테마를 입힌 게 아니다. 대기실은 여전히 초록 계열 옛
> 스타일이다 — 카드테이블 문법에 맞춰 다시 그리는 일은 `NEXT_SESSION.md` §5-1에 남아 있다.

### 실측 도구도 함께 늘렸다

`measureLobby.mjs`가 4단계 → **6단계**를 잰다. 새로 추가한 두 상태는 이번에 생긴 것들이다.

- `create+clash` — 두 팀 이름 칸에 같은 값을 넣어 경고를 띄운 상태. **경고가 실제로 떴는지까지
  확인한다** — 안 뜬 채로 재면 "문제 없음"으로 보이지만 아무것도 재지 못한 것이다(§4-2와 같은 함정).
- `join+invite` — `/?room=ABCD`로 새로 열어 참가 폼이 열리고 코드가 채워졌는지까지 확인한다.

기준값은 `NEXT_SESSION.md` §2-6.

### 검증

§1-4의 네 가지(서버 테스트 52/52, 서버 빌드, 클라이언트 빌드, 루트 tsc) + 로비 실측
3뷰포트 × 6단계 전부 통과. 추가로 헤드리스 두 탭으로 **방 만들기 → 초대 링크로 참가 →
채팅 왕복**까지 실제로 돌려 콘솔 에러가 없음을 확인했다(닉네임을 비워두고 시작해 무작위
이름이 그대로 쓰이는 흐름도 이때 함께 검증됐다).

## 7. 폼 백보드 — 나무 액자 + 네 귀퉁이 장식 (2026-09-04 후반)

폼(혼자 놀기·방 만들기·방 참가하기)의 배경이 **반투명 흰 카드 → 나무 액자 + 네 귀퉁이
장식**으로 바뀌었다. 배경이 카드테이블 일러스트가 되면서 흰 사각형이 겉돌았기 때문이다.

에셋은 `client/public/ui/`에 있고, 교체 절차와 실측표는
[`client/public/ui/README.md`](client/public/ui/README.md)에 정리해뒀다.

| 파일 | 비고 |
| --- | --- |
| `back_board.png` | **원본 PNG를 손대지 않고** 그대로 쓴다(444KB) |
| `corner_{tl,bl}.webp` | 2026-09-04에 새 그림으로 교체 |
| `corner_{tr,br}.webp` | 아직 이전 스타일 — 교체 요청이 없었다 |
| `score_board.webp` | 점수판용. 아직 미사용 |

### 되돌리면 조용히 깨지는 것 — 다섯 가지

**① 액자와 귀퉁이 장식을 한 장으로 합치지 말 것.**
`border-image`의 모서리 조각은 언제나 `border-width` × `border-width`(40px 남짓)로
그려진다. 210px짜리 장식을 합성했더니 그 안에 통째로 압축돼 잎·버섯·도토리가 **초록
얼룩**이 됐다. 액자는 `::before`(9분할), 장식은 `::after`(고정 크기 배경 4장)다.

**② 백보드의 투명 여백은 네 변이 다르다.**
상 78 / 하 67 / 좌 57 / 우 56px(1024² 기준). 그만큼 `::before`를 바깥으로 밀어야 나무가
카드 모서리에 맞는데, 한 값으로 뭉뚱그리면 위쪽이 22px·왼쪽이 16px씩 어긋나 액자가
한쪽으로 쏠린다. `--board-mt/mr/mb/ml` 네 값으로 나눠 준다.
**그림을 교체하면 이 네 숫자를 다시 재야 한다.**

**③ 슬라이스에 양피지를 많이 넣지 말 것.**
슬라이스가 크면 그 부분이 변 조각에서는 압축되고 가운데 조각에서는 확대되어, 둘이 만나는
자리에 양피지 톤이 어긋난 **이음매**가 생긴다(`120`으로 뒀다가 겪었다). 지금 `140`은
여백 + 나무 + 둥근 모서리가 겨우 들어가는 값이다.

**④ 글자 크기는 `--text-*`로 못 바꾼다.**
Tailwind v4가 `.text-sm`을 `calc(.875rem * var(--font-scale))`로 **인라인 컴파일**해
`--text-sm`을 읽지 않는다. 동적으로 남는 것은 `--font-scale`뿐이다. 그쪽을 쓰되, 사용자의
글씨 크기 설정(⚙️ 5단계)이 무시되지 않도록 `:root`의 거울 변수 `--font-scale-user`에
배수를 곱한다. 계수 `1.488` = 14pt ÷ (0.875rem × 12.8px/rem × 기본 배수 1.12).

**⑤ 폼 확대는 `zoom`이지 `transform: scale()`이 아니다.**
scale은 레이아웃 상자를 그대로 둬서 스테이지 밖으로 삐져나가고, `.stage-form`의 스크롤
판정과 `measureLobby` 실측이 눈에 보이는 것과 어긋난다. 그리고 **규칙을 펼치면 배율을
1로 되돌린다** — 그때는 카드가 이미 스테이지 폭을 다 쓰고 있어(1280에서 상한 1.04) 더
키울 자리가 없고, 렌더 폭이 그대로인 채 글자만 커지면 규칙 라벨이 다시 잘린다.

### 함께 들어간 연출

- **게임 규칙을 펼치면 아래가 아니라 오른쪽으로 자란다.** 이 로비에서 늘 모자란 것은
  세로였고 가로는 1280에서 380px이 놀고 있었다. 왼쪽 열(입력·버튼) / 오른쪽 열(규칙)
- **패널 주사선** — hover한 버튼에만. 위로 흐르고 깜빡인다.
  ⚠️ 깜빡임을 `opacity`가 아니라 `filter: opacity()`로 걸어야 한다. 애니메이션은 같은
  속성의 transition을 즉시 덮어써서, `opacity`를 흔들면 fade-in이 통째로 사라진다
- **배경 암전** — 단계를 고르면 테이블 배경에 검정 0.6.
  ⚠️ `z-index: -1`이 부모 밖으로 새지 않는 것은 `.lobby-table`이 `transform`으로 이미
  쌓임 맥락을 만들고 있어서다. 그 transform을 걷어내면 어둠이 배경 뒤로 숨는다

### 검증용 빌드는 dev 서버와 분리한다

"CSS를 고쳤는데 화면이 그대로"가 **네 번** 반복됐는데, 진짜 원인은 Turbopack이 아니라
**검증용 `next build`가 dev 서버와 `.next/`를 공유한 것**이었다. dev 서버가 10:46:23에
시작했는데 `.next/dev/`의 수정 시각이 10:47에 멈춰 있었다.

```bash
# 검증용 — .next/를 건드리지 않는다. WS 주소를 빼면 그 화면에서만 방이 안 만들어진다
NEXT_PUBLIC_WS_URL=ws://localhost:8080 NEXT_DIST_DIR=.next-verify npx next build
npm run dev:clean                              # 그래도 낡았으면 .next 삭제 후 dev
```

⚠️ 검증 빌드는 `client/tsconfig.json`을 자동 수정한다(그 폴더의 타입 경로를 끼워 넣음).
**커밋 전 `git checkout -- client/tsconfig.json`으로 되돌릴 것.**

### 검증

서버 테스트 52/52, 서버 빌드, 루트 `tsc --noEmit`, 클라이언트 빌드 전부 통과.
로비 실측 3뷰포트 × 6단계 문제 0건(기준값은 `NEXT_SESSION.md` §2-6).

## 8. upstream 2차 병합 — 관전자 자리 (2026-09-05)

upstream 12커밋을 병합했다(관전자, 시작 공유 카드 2장, 결과 화면 개편, 축제 배지,
하이드레이션 경고 제거 등). 전체 그림은 [`NEXT_SESSION.md`](NEXT_SESSION.md) §5-0에 있고,
여기에는 **로비를 어떻게 건드렸는지**만 적는다.

**충돌은 `client/app/page.tsx` 한 건이었다.** upstream은 게임 화면·서버를, 이쪽은 로비를
건드리기 때문이다. 해결은 1차와 같다 — upstream의 옛 로비 셸을 버리고 `data-stage` 구조를
지킨 채 그쪽 변경만 옮긴다. 이번에 옮긴 것은 `Team` → `Seat` 타입 전환, `spectatorSeat`
파생값, 자리 선택 UI, 방장이 관전석일 때의 팀 이름 라벨이었다.

`useWebSocket`은 이미 `Seat`을 받도록 자동 병합돼 있어 손댈 필요가 없었고, 게임 화면
(`app/room/[roomId]/page.tsx`)도 `sessionStorage`에서 읽은 자리가 `'spectator'`면 `myTeam`을
`null`로 두는 처리가 upstream에 들어 있었다. 로비만 맞추면 끝나는 구조였다.

### 로비 폼이 두 번 넘쳤다 — 둘 다 실측으로만 드러났다

**① 자리 버튼이 셋이 되면서 "관전자"가 접혔다.** 규칙을 펼치면 이 줄이 2열의 왼쪽
(약 240px)에 들어가는데, `whitespace-nowrap`이 없으면 "관전 / 자"로 접혀 줄 높이가
늘어난다. 카드가 **486 → 513px**. 화면으로는 버튼 하나가 조금 두꺼워 보일 뿐이라
스크린샷을 열어보기 전에는 원인을 짚기 어려웠다. `gap-1.5` + `px-1` + `whitespace-nowrap`.

**② 관전자 안내를 버튼 아래에 붙였더니 넘쳤다.** 1280×900에서 **534 → 560px**, 390×844에서
**530 → 556px**로 둘 다 폼이 스크롤했다. §6의 팀 이름 충돌 경고와 **똑같은 실패**다 —
이 로비에서 폼에 한 줄을 더하는 것은 거의 항상 실패한다.

그래서 같은 해법을 썼다: `FormCard`의 `description`을 갈아끼운다. 높이 증가 0.

> ⚠️ **충돌 경고와 관전자 안내는 그 한 줄을 나눠 쓴다.** 충돌 쪽이 우선한다(제출을 막는
> 오류이고, 관전자 안내는 그냥 안내다). 그래서 **충돌을 켜둔 채로는 관전자 안내를 볼 수
> 없다** — `measureLobby.mjs`에서 이걸 모르고 두 상태를 겹쳐 재다가 "관전자 안내 안 뜸"으로
> 세 뷰포트가 전부 실패했다. 지금은 충돌을 먼저 풀고 관전석을 고른다.

`measureLobby.mjs`가 6 → **7단계**를 잰다(`create+spectator` 추가).
**`방 만들기+관전`은 `방 만들기+규칙`과 값이 같아야 한다** — 커졌다면 누군가 안내를
새 줄로 되돌린 것이다.

### 검증

서버 테스트 **63/63**(관전자 테스트 포함), 서버 빌드, 클라이언트 빌드, 루트 `tsc` 통과.
로비 실측 3뷰포트 × 7단계 문제 0건. 추가로 헤드리스 두 탭으로 **방 만들기 → 초대 링크로
관전 참가 → 채팅**까지 돌려, 시작이 "양 팀에 각각 한 명 이상 있어야 해요. (관전자는 인원에
들어가지 않아요)"로 올바르게 막히는 것까지 확인했다.

## 9. 나무 액자를 대기실·결과 창까지 (2026-09-05)

액자(`back_board.png` + 귀퉁이 장식 4장)는 로비 폼에만 있었다. 그래서 사용자가 지나가는
순서대로 보면 **방을 만든 순간과 게임이 끝난 순간에만 흰 카드가 튀어나왔다.** 같은 액자를
그 두 곳에 더 둘렀다.

### 액자를 그리는 규칙을 폼에서 떼어냈다

`.lobby-form-board` 하나에 "액자를 어떻게 그리는가"와 "폼을 어떻게 배치하는가"가 섞여
있었다. 앞의 것만 `.lobby-form-board, .board-frame` 공유 선언으로 뺐다.

- **9분할 숫자가 한 곳뿐이 된다.** `--board-slice: 140`과 네 방향 투명 여백 보정
  (`--board-mt/mr/mb/ml`)은 `ui/README.md`가 "그림을 교체하면 다시 재라"고 말하는 값들이다.
  복사해 두 벌로 만들었으면 다음에 그림을 갈 때 한쪽만 고치고 끝났을 것이다.
- `.board-frame`은 `zoom`·`max-width`·규칙 2열 그리드를 **갖지 않는다.** 배치는 쓰는 쪽이
  `className`으로 준다.
- 그림 경로는 여전히 `lobbyAssets.ts` 한 곳에서만 나온다 — `BoardFrame`
  (`components/ui/BoardFrame.tsx`)이 커스텀 속성으로 흘려보낸다.

### `--orn-size`는 폼보다 작아야 한다

폼의 `126px`은 **`zoom: 1.75`가 걸린 상태**에서 고른 값이다. 확대가 없는 곳에 그대로 쓰면
과하다 — 결과 창의 "행동 사용 통계"는 높이가 약 210px인데 위아래 장식이 서로 닿아
**카드 가운데가 잎으로 막혔다.** 96px로 내려 180px 남짓한 카드까지 견디게 했다.

`--board-edge`도 40 → 34px로 낮췄다. 키가 낮은 카드에서 40px 나무테는 안쪽보다 테두리가
더 눈에 띈다.

### 회색 상자는 양피지 위에서 겉돈다

대기실 안쪽은 `bg-gray-50`·`bg-white`·`text-gray-400`이었다. 양피지 위에 그대로 두면 종이
조각을 붙여 놓은 것처럼 보인다. `.board-panel`(따뜻한 반투명 흰색 — 아래 양피지 결이 비친다)
과 `text-board-ink`/`text-board-muted`로 바꿨다.

> **로비 폼용 `GameRulesFields`는 일부러 건드리지 않았다.** 실측이 걸려 있고, 대기실이 쓰는
> 것은 같은 파일의 `GameRulesInputs`·`RuleSummary`뿐이다. 그 둘만 바꿨다(색만 바뀌므로
> 레이아웃 영향 없음). `RuleSummary`는 `bg-gray-50 rounded-xl` → `board-panel`이라 테두리
> 1px이 붙지만, 이 컴포넌트는 대기실에만 나오므로 로비 실측과 무관하다.

### 검증

공유 선언으로 뺀 뒤 **로비 실측을 다시 돌려 §2-6 기준값과 한 픽셀도 다르지 않음을 확인**했다
(3뷰포트 × 7단계, 문제 0건). 서버 테스트 63/63, 서버 빌드, 클라이언트 빌드, 루트 `tsc` 통과.
대기실(1440·390)과 결과 창은 실제로 띄워 눈으로 확인했다 — 결과 창은 싱글 게임을 끝까지
돌려서 봤다.

## 10. 결과 화면 메달·월계수 (2026-09-05)

받은 스프라이트 시트(1024², 월계수 3종 + 메달 1·2·3등 + 팻말 등)에서 세 조각을 떼어
결과 화면에 넣었다. 화면 쪽 규칙은 `NEXT_SESSION.md` §2-8, 에셋 출처와 좌표는
[`client/public/ui/README.md`](client/public/ui/README.md)에 있다. 여기에는 **자르는 과정에서
겪은 것**만 적는다.

### 사각형으로 자르면 이웃이 딸려 온다

처음엔 눈대중으로 영역을 잡아 잘랐는데 두 번 틀렸다.

1. 월계수는 왼쪽 이웃(빈 월계수, `x1=346`)이 2px 딸려 들어갔다 — 영역을 345부터 잡아서다.
2. 금메달은 오른쪽 밀 이삭이 잘렸다 — 실제 `x1`은 700인데 640에서 끊었다.

그래서 **연결 성분 라벨링**으로 실제 오브젝트 경계를 먼저 뽑았다. 그런데 정확한 경계로
잘라도 금메달 오른쪽 위에 **도토리 조각**이 남았다 — 도토리 덩어리(`682,356~`)와 금메달
(`~700,586`)의 경계상자가 겹쳐서, 사각형으로 자르는 한 피할 수 없다.

최종 방법: 사각형이 아니라 **덩어리 단위로 남긴다.** 경계상자가 대상 영역 안에 완전히
들어가는 덩어리만 남기고 나머지는 알파 0. 배경(거대한 흰 덩어리)도 같은 규칙에 걸린다.

> **흰 배경을 전역 임계값으로 지우지 말 것.** 책 페이지·버섯 흰 점·양피지처럼 안쪽에도
> 밝은 부분이 있어 구멍이 뚫린다. 덩어리 단위로 지우면 그 문제가 아예 생기지 않는다.
> (가장자리 흰 테두리는 밝기에 비례해 알파를 낮춰 따로 다듬었다.)

### 원본 시트는 저장소에 없다

첨부 이미지는 디스크에 저장되지 않고 세션 트랜스크립트에 base64로만 들어 있다
(`NEXT_SESSION.md` §4-5). 다시 자를 일이 생기면 거기서 꺼내야 한다. 시트에는 **동메달 "3"**,
빈 월계수 2종, 나무 팻말 2종, 도토리·버섯, 원형 아이콘 2종, 큰 나무 액자가 아직 남아 있다.

### 문장은 판이 스스로 자리를 내고, 귀퉁이 장식과 같은 층에 있다

`.board-crest`는 `translate(-50%, -50%)`로 액자 윗변을 반씩 물고 앉는다(폭 53px 기준
위 22px / 아래 22px). 위로 삐져나온 절반이 **바로 위 요소와 겹치므로**,
`.board-frame:has(> .board-crest)`가 그만큼 `margin-top`을 낸다. 계수 `0.42`는 그림
비율(320:268 ≈ 0.84)의 절반이다 — **다른 비율의 그림으로 갈면 이 값도 함께 고쳐야 한다.**

`z-index: -1`은 **`.board-frame::after`(귀퉁이 장식 4장)와 같은 값**이다. 액자 그림
(`::before`, -2)보다 위, 판 안의 글자보다 아래라서 "액자에 붙은 장식"으로 읽힌다.
처음엔 `1`로 뒀는데 그러면 글자와 같은 층이 되어 판 **안쪽** 요소가 된다.

### 크기는 세 번 조정했다

| 회차 | 메달 | 문장 |
| --- | --- | --- |
| 처음 | `h-24` | `132px` |
| 1차 (1.5배 / 0.4배) | `h-36` | `53px` |
| **2차 (0.9배 / 0.7배)** | **`h-[8.1rem]`** | **`92px`** |

1차에서 문장이 너무 작아져 되돌렸다. 2차의 `0.7배`는 **축소 전 값(132px)** 기준이다.

> ⚠️ **이 프로젝트의 루트 글꼴은 16px가 아니라 12.8px다.** `rem`이 흔한 감각과 다르게
> 환산되므로 크기를 셀 때 주의할 것 — `h-36`은 144px이 아니라 **115px**이고, 지금의
> `h-[8.1rem]`은 **약 104px**이다. (앞선 커밋 메시지에 144px·321px로 적은 것은 16px
> 기준으로 잘못 계산한 값이다.)

`max-w-full object-contain`은 좁은 화면 방어선이다. 지금 크기로는 금메달 폭이 약 231px이라
390px 화면(콘텐츠 폭 326px)에 여유 있게 들어가지만, 더 넓은 비율의 그림으로 갈면 넘칠 수 있다.

문장을 92px로 키우자 **아래 절반이 판 안으로 깊이 들어와 "행동 사용 통계" 제목을 덮었다.**
그래서 `:has(> .board-crest)`에 `margin-top`(위 요소와 겹치지 않게)뿐 아니라
`padding-top`(맨 윗줄 글자와 겹치지 않게)도 함께 준다. 둘 다 `--crest-w`에 비례한다.

### 검증

승리(금메달)·패배(은메달) 두 경우를 각각 게임을 끝까지 돌려 눈으로 확인했다. 패배는 그냥
두면 나왔고, 승리는 봇이 매 턴 상표토끼(체력 회복)를 쓰게 해서 재현했다. 문장 두 개가
제자리(판 윗변, 정확히 절반씩)에 있고 페이지 스크롤이 생기지 않는 것도 함께 쟀다.
로비 실측은 §2-6 기준값 그대로다.

## 11. 대기실 스크롤을 액자 안쪽으로, 귀퉁이 장식의 검은 칠 제거 (2026-09-05)

### 스크롤이 액자 밖에 있었다

`page.tsx`의 바깥 래퍼가 `overflow-y-auto`였다. 그래서 대기실을 내리면 **나무 액자까지
통째로 밀려 올라가** 판이 화면 밖으로 나갔다. 지금은 액자가 제자리에 서 있고 양피지
안쪽만 오르내린다 — 래퍼에서 `overflow-y-auto`를 빼고, `BoardFrame`을 `h-full flex flex-col`로
둔 뒤 그 안에 스크롤 컨테이너를 넣었다.

> ⚠️ **여기서 한 번 깨졌다.** 스크롤 컨테이너를 `flex flex-col`로 만들자 **채팅창이
> 선 하나로 눌려 사라졌다.** 높이가 정해진 flex 열에서는 자식이 기본값(`flex-shrink: 1`)대로
> 내용보다 작게 찌그러지기 때문이다 — `h-48`짜리 대화 목록이 0에 가깝게 줄었고, 넘치지
> 않으니 스크롤도 생기지 않았다. `[&>*]:shrink-0`으로 **자식이 제 높이를 지켜야** 넘치고,
> 넘쳐야 컨테이너가 스크롤한다.

### 귀퉁이 장식에 검은 칠이 있었다

받은 `corner_*.webp` 네 장은 **잎사귀 사이 빈틈이 투명이 아니라 검게 칠해져** 있었다
(`corner_tl`은 덩굴 고리 안쪽이 통째로 검은 타원, 436px). 양피지 위에서는 그대로 얼룩이다.

윤곽선까지 지우면 안 되는데 색이 확실히 갈렸다 — **검은 칠은 max(r,g,b) 0~3, 윤곽선은
70~145.** 30 이하만 투명으로 바꾸고 경계의 안티에일리어싱 화소(30~90)는 밝기에 비례해
알파를 낮췄다. 네 장 모두 30 이하 덩어리가 0개가 됐다. 자세한 값은
[`client/public/ui/README.md`](client/public/ui/README.md).

> `sharp(경로)`로 읽어 **같은 파일에 다시 쓰면 Windows에서 잠긴다**(`EPERM` → `UNKNOWN`).
> `fs.readFileSync`로 버퍼에 담아 `sharp(buffer)`로 넘기면 된다.

### 검증

대기실 1440×800·390×844에서 **안쪽만 스크롤하고 액자는 제자리(y=19 고정)** 임을 실제로
쟀다. 바깥은 스크롤하지 않는다. 결과 창은 메달·문장 크기를 재고 눈으로 확인했다.
로비 실측은 §2-6 기준값 그대로.

## 12. 방 만들기 폼 5:5 재배치 + 글자 기본값 복귀 (2026-09-06)

규칙을 펼쳤을 때의 2열 배치를 `0.85fr : 1.15fr` → **`1fr : 1fr`** 로 바꾸고, 무엇이 어느
열에 가는지를 다시 갈랐다.

| 열 | 무엇 | 뜻 |
| --- | --- | --- |
| 왼쪽 | 머리말 · 닉네임 · 우리 팀 이름 · 상대 팀 이름 | **내가 적는 것** |
| 오른쪽 | 자리 선택 · 게임 규칙 · 제출 버튼 | **내가 고르는 것** |

예전에는 오른쪽이 넓었다(규칙 안에서 다시 2열 격자를 쓰기 때문). 지금은 자리 선택이
오른쪽으로 넘어가면서 양쪽 무게가 맞아 5:5가 자연스럽다.

`TeamSelect`를 `FormCard`의 `children`에서 **`side` 슬롯으로 옮겼다.** grid-area를 새로
잡는 것보다 이쪽이 낫다 — `side`는 이미 오른쪽 열이고, **접혀 있을 때는 DOM 순서가
그대로라 화면의 차례가 예전과 같다**(닉네임 → 팀 이름 → 자리 선택 → 규칙 → 버튼).
`.lobby-form-side`에 flex 열 + gap을 줘 자리 선택과 규칙이 벌어지게 했다.

버튼은 `grid-area: 3 / 1` → `3 / 2`(오른쪽 열 맨 아래)로 옮겼고, `side`는 `1/2/4/3` →
`1/2/3/3`(1~2행)으로 줄여 그 자리를 내줬다.

### 글자를 14pt 고정에서 기본값으로 되돌렸다 — 그러자 확대 상한이 내려갔다

`.lobby-form-board`의 `--font-scale: calc(var(--font-scale-user) * 1.488 / var(--form-zoom))`
을 지웠다. 이제 글자도 액자와 함께 `--form-zoom`만큼 커진다.

> ⚠️ **글자 크기와 확대 상한은 한 몸이다.** 글자가 zoom을 나눠 되돌리지 않게 되면서
> 1920×1080의 "초대 링크 참가" 폼(규칙이 없어 2열이 안 되고 zoom을 그대로 받는다)이
> **657px로 넘쳤다.** `--form-zoom`을 1.75 → **1.6**으로 내려 601px로 맞췄다.
> **둘 중 하나를 건드리면 다른 하나를 반드시 다시 재야 한다.**

`--text-*`를 재정의하지 말라는 규칙(§7 ④)은 그대로 유효하다 — 다시 글자를 키우게 되면
`--font-scale`과 `:root`의 거울 변수 `--font-scale-user`를 써야 한다. 지금은 그 배수가
없을 뿐이다.

### 검증

실측이 크게 내려갔다(§2-6의 기준값을 갱신했다).

| | 전 | 후 |
| --- | --- | --- |
| 방 만들기+규칙 | 706×486 | **706×365** |
| 싱글+규칙 | 706×343 | **706×303** |
| 참가+초대(1920) | 627×623 | **573×601** |

3뷰포트 × 7단계 문제 0건, 콘솔 에러 없음. 서버 테스트 63/63, 빌드·타입검사 통과.

> 카드가 짧아지면서 규칙을 펼친 화면의 양피지 아래쪽이 꽤 비어 보인다. 규칙이 펼쳐진
> 동안에는 `--form-zoom`이 1로 고정되어 있어(카드가 이미 스테이지 폭 738 중 706을 쓴다)
> 더 키울 자리가 가로로 없기 때문이다. 세로만 채우려면 배치를 더 손봐야 한다 — 남은 일.

## 13. 결과 화면 2열 + UI 아이콘 5종 (2026-09-07)

### 결과 화면을 좌우 2열로

체력표와 행동 사용 통계가 위아래로 쌓여 있었다. 둘 다 `max-w-2xl`이라 넓은 화면에서
좌우가 크게 비고 세로로만 길어졌다. **같은 높이의 좌우 2열**로 바꿨다.

- 높이는 `grid`의 `items-stretch`가 맞춘다. 액자가 그 높이를 따라가려면
  **`BoardFrame`에 `h-full`** 이 있어야 한다
- 좁은 화면(`<md`)에서는 1열로 떨어져 예전 세로 배치가 그대로 된다
- ⚠️ **판 안쪽 격자는 2열 → 1열로 되돌려야 한다.** 2열 안에 또 2열을 넣으면 팀 이름이 잘린다

실측: 두 액자 `400×407` 동일, 같은 y(396), 좌우(x 390 / 810), 페이지 스크롤 없음.
버튼과의 간격 19px > 장식 돌출 10px이라 겹치지 않는다.

### 아이콘 시트에서 5개를 잘라 이모지를 대신하게

| 자리 | 이전 → 이후 |
| --- | --- |
| 대기실 시작 버튼 | 👑 → `icon_start` |
| 방 만들기 폼 제목 | 🏠 → `icon_create` |
| 결과 화면 액자 상단 문장(`BoardCrest`) | 월계수(`laurel_book`) → `icon_rank` |
| 로비 "게임 방법" | 📖 → `icon_help` |
| 우하단 설정 + 게임 규칙 접기/펼치기 2곳 | ⚙️ → `icon_cog` |

**① 글씨는 그림에 넣지 않는다.**
시트에는 아이콘 아래에 라벨이 함께 그려져 있었지만 그 부분은 잘라내 버렸다. 라벨은 전부
HTML이다 — 글꼴을 바꾸거나 문구를 고칠 때 그림을 다시 만들지 않아도 되고, 스크린리더도
읽는다(`img`는 `alt=""` + `aria-hidden`, 읽히는 것은 옆의 HTML 텍스트뿐).

**② 크기는 `em`으로 잡는다.**
`.ui-icon`이 `1.6em`이다. 글자 크기를 따라가므로 글씨 크기 설정(⚙️ 5단계)을 바꿔도
아이콘만 따로 놀지 않는다. `px`로 고정하면 그 설정에서 어긋난다.

**③ 배경은 색상 키가 아니라 flood fill로 지운다.**
시트 배경은 베이지(≈233,212,180)인데, 고정 색상 키를 쓰면 아이콘 안의 밝은 부분
(책·양피지)까지 뚫린다. **테두리에서 연결된 영역만** 지우고 가장자리 한 겹은 alpha 140으로
둬 계단을 덜었다.

**④ 글자가 그림에 붙어 나오는 것이 있다.**
`icon_start`만 아래 "게임 시작" 글씨가 원의 빛무리와 이어져 한 덩어리(301×341)로 잡혔다.
높이를 원의 지름인 295로 손수 줄였다. 나머지 4개는 연결 성분이 그대로 아이콘 상자였다.
**새 시트를 쓸 때도 잘라낸 결과를 눈으로 확인할 것.** 자른 좌표표는
[`client/public/ui/README.md`](client/public/ui/README.md)에 있다.

`FormCard`의 `title`을 `string` → `React.ReactNode`로 넓혔다(제목에 아이콘을 넣기 위해).

### 검증

server test 63/63, server build, 루트 `tsc --noEmit`, client build 통과.
로비 실측 3뷰포트 × 7단계 문제 0건. **결과 화면은 혼자 놀기를 끝까지 자동 플레이해**
실제 화면으로 확인했다.

> ⚠️ 결과 화면을 헤드리스로 확인할 때는 `.place-tile-active`를 누르는 것만으로 부족하다.
> 행동 선택 단계의 **"턴 마치기"** 버튼까지 눌러야 턴이 넘어간다. 20턴을 다 도는 데
> 약 3분 걸리므로 반복 횟수를 넉넉히(900회) 잡을 것.

## 14. 그림 버튼 3종과 기본 글꼴 (2026-09-08)

로비 버튼의 겉모습을 하루 종일 다듬었다. **형태가 세 번 바뀌었고, 뒤로 갈수록 "그림에
글씨를 넣지 않는" 쪽으로 갔다.** 새 버튼을 만들 때 어느 쪽인지부터 고르면 된다.

| | 그림 | 글씨 | 크기 지정 | 클래스 |
| --- | --- | --- | --- | --- |
| 아이콘 버튼 | 아이콘 한 장 | 그림에 없음 → `aria-label`만 | 정사각형 한 변(px) | `.icon-button` |
| 가로 팻말 | 글씨까지 그려진 완성 팻말 | **그림 안에 있음** → 라벨 쓰면 두 번 보인다 | 가로만, 세로는 비율 | `.bar-button` |
| **나무판 팻말** | 빈 나무판(9분할) | **HTML로 얹는다** | 글자 길이를 따라 늘어남 | `.plank-button` |

**세 번째가 이겼다.** 문구가 상태에 따라 바뀌는 버튼("준비" ↔ "준비 완료 ✓ (누르면 취소)",
"규칙 적용" ↔ "적용됨 ✓")은 글씨가 박힌 그림으로는 만들 수 없기 때문이다. 그 결과
`btn_start_bar`·`btn_solo_bar`·`icon_start` 세 에셋과 `BarButton` 컴포넌트가 **미아가 됐다**
(되돌릴 수 있게 남겨 뒀지만 174KB가 매 접속마다 프리로드된다 — §16 참조).

### 되돌리면 조용히 깨지는 것 — 다섯 가지

**① 9분할의 슬라이스와 테두리 두께는 비율을 맞춘 짝이다.**
`border-image`의 모서리 조각은 **언제나 `border-width` 크기로 그려진다.** 그래서
가로 `95 → 28px`(0.295배)와 세로 `40 → 12px`(0.300배)의 배율이 같아야 마구리가 안 눌린다.
**한쪽 숫자만 고치면 끝이 찌그러진다** — `back_board`에서 겪은 것과 똑같은 함정이다.
실측으로 "준비" 428×63, "규칙 적용" 546×61 — 폭이 100px 넘게 달라도 마구리는 그대로다.

**② 그림 버튼의 크기는 `em`이 아니라 px다.**
로비 폼의 세로 여유가 몇 px 단위라, 글씨 크기 설정(⚙️ 5단계)에 버튼까지 따라 커지면
폼이 넘친다. 반대로 `.ui-icon`(글자 옆 작은 그림)은 `em`이어야 글자와 함께 움직인다.

**③ 배경 없는 버튼에는 `box-shadow`가 안 통한다.**
투명한 사각형 상자에 걸려 아무 데도 그려지지 않는다. **모양을 따라가는 `drop-shadow`**를 쓴다.

**④ 팻말 글씨의 그림자는 blur 0이어야 한다.**
1px만 줬는데도 획 주변이 뿌옇게 번져 읽기 나빠졌다. 두 그림자 모두 **아래로**,
짙은 쪽이 1px, 옅은 쪽이 2px. 또렷한 한 겹으로 어긋나 있을 때 새겨진 것처럼 보인다.
hover에 `transform`도 걸지 않는다 — 판이 떠오르면 글씨도 함께 움직여 그 느낌이 깨진다.

**⑤ 기본 글꼴은 `woff2`다.**
받은 폴더에 넷이 있는데 용량이 열 배 가까이 다르다 — ttf 3.8MB · otf 1.6MB · woff 906KB ·
**woff2 392KB**. 한글 글꼴은 원본이 무거워 그 차이가 첫 접속 대기시간에 그대로 얹힌다.
`next/font/local`로 실어 preload·해시 캐시·대체 글꼴 크기 보정을 Next가 하게 했다.
빌드 산출물(`.next/static/media`)에 들어가므로 **런타임에는 `public/fonts`가 필요 없다.**

> ⚠️ **글꼴이 바뀌면 글자 높이가 달라져 로비 폼이 몇 px 움직인다.** 실제로 방 만들기
> 카드가 404 → 405px가 됐다. 글꼴을 손대면 `measureLobby.mjs`로 반드시 다시 잴 것.

### 배경 제거는 매번 기준이 다르다

첨부 이미지는 **JPEG로 도착한다.** 그래서 원본이 투명 PNG였어도 투명한 자리가 무언가로
칠해져 온다. 이 날만 세 번 다른 기준이 필요했다.

| 받은 것 | 투명한 자리가 | 쓴 기준 |
| --- | --- | --- |
| 아이콘 시트 | 검게 | 테두리에서 이어진 `max(r,g,b) ≤ 20` flood fill |
| 가로 팻말 | 따뜻한 어두운 글로우(53~96) | 뒤집어서 **"밝기 130 이상 = 나무판"** |
| 빈 나무판 | 희게 | 테두리에서 이어진 `min(r,g,b) ≥ 232` flood fill |

**지난 기준을 그대로 가져다 쓰면 아무것도 안 지워지거나 그림이 뚫린다.** 모서리와
중앙선의 실제 화소값을 먼저 찍어보고 문턱을 정할 것.

> ⚠️ **"가장 큰 밝은 연결 성분"으로 대상을 고르지 말 것.** 팻말이 짙은 나뭇결로 쪼개져
> 안쪽 그림 띠가 더 큰 덩어리로 잡히면서 **나무테와 "게임 시작" 글씨가 통째로 잘려
> 나갔다.** 통나무집 안쪽까지 구멍이 뚫렸다. **배경 쪽에서 flood fill로 접근하면**
> 대상이 몇 조각이든, 안쪽이 아무리 짙든 그대로 살아남는다.

> ⚠️ 판 바깥의 짙은 윤곽선은 밝기로 갈리지 않는다 — 윤곽선 49~57인데 그 **바깥** 배경이
> 95~100으로 오히려 밝다. 밝기가 아니라 **거리변환**으로 되살렸다(12px까지 불투명,
> 14px에서 0).

> ⚠️ **잘라낸 결과는 반드시 양피지색과 초록 펠트 두 배경 위에 합성해 눈으로 볼 것.**
> 2026-09-05에 이 확인을 빼먹어 검은 타원이 그대로 배포된 적이 있다.

### 스크롤바 — 보이는 굵기와 잡히는 폭을 따로 둔다

홈(track)은 투명, 손잡이는 얇은 알약, 양끝 화살표는 제거했다.

**손잡이를 정말 2px로 만들면 마우스로 잡을 수가 없다.** 그래서 스크롤바 자체는 10px로
두고, 손잡이에 **투명한 테두리 4px**을 둘러 색칠 영역만 2px로 보이게 했다
(`background-clip: padding-box`가 있어야 테두리 자리에 배경이 안 칠해진다).
hover하면 테두리가 2px로 줄어 6px(radius 2px)이 되는데, **잡히는 폭 10px은 그대로**라
커서가 흔들려도 놓치지 않는다.

`::-webkit-scrollbar-button`을 0으로 두지 않으면 얇은 막대 위아래에 회색 사각형 둘이 남는다.
Firefox는 `scrollbar-width: thin`과 `scrollbar-color`까지만 받는다 — 그쪽은 그것으로 충분하다.

> ⚠️ **헤드리스 Chrome은 webkit 스크롤바를 그리지 않는다.** 사진으로 확인할 수 없으니
> 실제 브라우저에서 볼 것. 빌드된 CSS에 6개 규칙이 들어간 것과 `scrollbar-color`가
> 실제 요소에 적용된 것까지는 확인했다.

### "게임 규칙 펼치기"는 하루에 세 번 바뀌었다

| | 동작 | 버린 이유 |
| --- | --- | --- |
| ① | 오른쪽으로 넓어지는 2열 (`51d6d24`~`8efe72e`) | 사용자가 세로로 되돌려 달라고 함 |
| ② | 세로로 자라되 확대를 1로 내리고 제출 버튼을 40px로 축소 | **펼치는 순간 카드가 눈에 띄게 쪼그라들었다** |
| ③ | **카드 크기를 그대로 두고 넘치는 만큼 스크롤** ← 지금 | — |

**①을 되돌리자 그것이 해결하던 문제가 그대로 돌아왔다.** 가로 2열은 "세로로만 자라면
스테이지를 넘친다"는 이유로 만들어진 것이었고, 되돌리니 1280×900에서 카드가 583px,
스테이지는 590px — 실측 3건이 넘쳤다. **되돌림은 공짜가 아니라 그 이유를 다시
감당하겠다는 결정이다.** `git log -S`로 도입 커밋을 찾으면 커밋 메시지에 이유가 있다.

③은 코드를 더한 게 아니라 **그동안 넣어 둔 보정을 지우는 일**이었다 —
`.stage-form`이 원래부터 `overflow-y: auto`다. 규칙 입력도 2열 압축을 풀어 한 항목씩
세로로 놓았다(라벨이 더는 잘리지 않는다).

> ⚠️ **`measureLobby.mjs`도 함께 고쳤다.** 그대로 두면 규칙 펼침에서 매번 12건이 떠서
> 진짜 문제를 덮는다. 이제 `+rules`/`+clash`/`+spectator` 라벨에서는 폼 스크롤을
> `yes(정상)`으로 표시하고 문제로 세지 않는다. **규칙을 접은 상태의 스크롤이 진짜
> 문제다** — `join+invite`(규칙이 없는 폼)가 그 대표다.
> 측정 도구가 늑대소년이 되면 다음 사람이 그 표를 안 믿는다.

### 배경 어둠은 폼이 열리는 단계에서만

`.lobby-table::after`가 검정 0.6을 덮는데, **"다같이 놀기"에서는 더 이상 어두워지지
않는다.** 거기는 아직 고르는 중이라, 그 단계에서 이미 어두워지면 곧바로 방 만들기를
눌러도 더 어두워질 것이 없어 무엇을 고른 순간인지 화면이 알려주지 못한다.
지금은 `solo`/`create`/`join`/`waiting` 넷에서만 덮인다.
실측 — home 0 / 다같이 놀기 0 / 방 만들기 0.6.

### 검증

server test 63/63, server build, 루트 `tsc --noEmit`, client build 통과.
로비 실측 3뷰포트 × 7단계 문제 0건. **두 브라우저로 실제 방을 만들고 참가해**
준비 → 준비 완료 → 방장 시작 버튼 활성화까지 확인했다.

---

## 15. Render 배포 구성 (2026-09-08)

`Dockerfile`·`.dockerignore`·`docker-compose.yml` 셋은 **upstream과 완전히 동일**해
손댈 것이 없었다(`git diff upstream/main`이 비어 있다). 배포를 막고 있던 것은 다른 데 있었다.

### `NEXT_PUBLIC_*`는 빌드 시점에 번들로 박힌다

런타임 환경변수가 아니다. 그래서 기존 구조는 **배포 도메인을 빌드 전에 알아야 했는데**,
Render는 서비스 이름이 이미 쓰이고 있으면 도메인 뒤에 임의 문자열을 붙인다
(`ip-card-battle-render.onrender.com`은 이미 upstream이 쓰고 있다).
한 글자만 어긋나면 **화면은 멀쩡히 뜨는데 방만 안 만들어진다** — 찾기 가장 어려운 고장이다.

그래서 클라이언트가 주소를 스스로 찾게 했다(`client/hooks/useWebSocket.ts`의 `resolveWsUrl`):

1. `NEXT_PUBLIC_WS_URL`이 있으면 그것
2. 없고 **개발 빌드**면 `ws://localhost:8080`
3. 없고 **프로덕션 빌드**면 이 페이지와 같은 호스트의 `/ws`

> **갈림길이 호스트가 아니라 빌드 모드인 것이 요령이다.** 로컬에서 Docker 이미지를 띄우면
> 브라우저 주소가 개발 때와 같은 `localhost:3000`인데 WS는 8080이 아니라 같은 포트의
> `/ws`에 있다. 호스트 이름으로는 이 둘을 가를 수 없다.

> ⚠️ 그 대신 **로컬 프로덕션 검증 빌드에 `NEXT_PUBLIC_WS_URL=ws://localhost:8080`이
> 필요해졌다** — 검증 서버(3200)에는 WS가 없고 8080에 따로 뜬다. 빼먹으면 그 화면에서만
> 방이 안 만들어진다.

### 검증은 Docker 없이 했다

이 PC에 Docker가 없어 이미지는 못 만들었다. 대신 **Dockerfile이 실행하는 그 명령**을
그대로 돌렸다.

```bash
NODE_ENV=production NEXT_DIST_DIR=.next-verify PORT=3300 \
  npx ts-node --transpile-only server.ts
```

클라이언트가 `ws://localhost:3300/ws`로 붙고, 방 생성·대기실·혼자 놀기 게임 화면까지
정상, 콘솔 에러 0건.

### 남은 한 단계 — 사람이 해야 한다

`render.yaml`(Blueprint)은 저장소에 있지만 **Render 쪽에서 그 저장소를 "선택"하는 단계는
대시보드에서 직접 해야 한다.** Render는 GitHub를 훑어 `render.yaml`을 찾아내지 않는다 —
사용자가 Render에게 "이 저장소를 봐라"라고 알려줘야 한다.

> 아직 연결 전이라는 근거: `gh api repos/Aringco/ip_card_battle_render/hooks`가 빈 배열이다.
> Render는 저장소를 연결할 때 push 알림용 웹훅을 등록한다.

**New + → Blueprint → `Aringco/ip_card_battle_render` → Apply** 한 번이면 되고,
그 뒤로는 `main`에 push할 때마다 자동 재배포된다. 넣을 환경변수는 없다.

> ⚠️ `plan: free`로 적어 뒀다. 무료 플랜은 15분간 요청이 없으면 잠들고 깨어나는 데 1분쯤
> 걸리며, **그때 WebSocket 연결이 끊긴다.** 실시간 대전 게임에는 체감이 크다.

---

## 16. 게임 방법 창 나무 판 + 규칙 펼침을 카드 안쪽 스크롤로 (2026-09-08 밤)

### 규칙을 펼쳐도 카드가 자라지 않는다

오후 작업(`ae5d586`)에서 "펼치면 카드가 세로로 자라고 넘치는 만큼 `.stage-form`이 스크롤한다"로
바꿨는데, 그러면 **나무 액자와 귀퉁이 장식까지 함께 밀려 올라가** 판이 화면 밖으로 나갔다.
대기실·게임 방법 창에서 이미 쓴 것과 같은 해법으로 통일했다 — **액자는 제자리, 안쪽만 스크롤.**

- `.lobby-form-board`에 `max-height: 100%`
- `FormCard`가 머리말·입력·규칙을 `.lobby-form-scroll`로 감싼다
- **제출 버튼은 스크롤 밖**에 남긴다. 규칙을 끝까지 내리지 않아도 늘 눌려야 한다

> ⚠️ **`max-height`를 `--form-zoom`으로 나누지 말 것.** 요즘 Chrome의 `zoom`은 퍼센트를
> **이미 확대분만큼 보정해서** 해석한다. 한 번 더 나눴더니 카드가 배율만큼(1.35배) 작아져,
> 스테이지 550px 자리에 413px짜리 카드가 덩그러니 떠 있었다. 화면을 안 봤으면 "문제 0건"
> 이라는 실측만 믿고 넘어갔을 것이다.

> ⚠️ 스크롤 컨테이너의 자식에는 `[&>*]:shrink-0`이 필요하다(§11에서 채팅창이 선 하나로
> 눌려 사라진 것과 같은 함정).

실측이 크게 내려갔다 — 펼친 상태가 뷰포트마다 **한 값으로 고정**된다(그 화면에서 카드가
가질 수 있는 최대 높이). 849 → **558**(1280), 1003 → **683**(1920), 621 → **538**(390).

### 게임 방법 창

받은 그림 3장에서 여섯 조각을 잘라 초록 카드를 나무 액자로 바꿨다. 자르는 방법과 좌표는
[`client/public/ui/README.md`](client/public/ui/README.md)에 있고, 여기에는 화면 쪽만 적는다.

- 판은 **9분할**(`.howto-board::before`, 슬라이스 110) — 창 비율이 그림(1289×652)과 달라
  통째로 늘리면 모서리 나무가 눌린다
- 네 귀퉁이는 `::after`에 **고정 크기 배경 4장**(로비 폼과 같은 방식). 그림마다 세로 비율이
  달라(320×277~305) `background-size: <폭> auto`로 둔다
- 스크롤은 `.howto-scroll`이 맡는다 — 액자·팻말·귀퉁이는 제자리에 남는다

> ⚠️ **팻말은 위·아래 양쪽으로 자리를 먹는다.** 판 바깥으로 절반(`margin-top`), 판 안쪽으로
> 절반(`padding-top`). 안쪽 몫을 빼먹었더니 첫 줄을 덮었다. 계수 `0.149`는 그림 비율
> (768:229)의 절반이라 팻말 폭만 바꿔도 따라온다.

> ⚠️ **닫기(✕)는 나무테 위에 앉는다.** 양피지용 갈색으로 뒀더니 어두운 나무에 묻혀
> 보이지 않았다 — 밝은 크림색 + 그림자로 바꿨다.

원본 그림에서 겪은 것 둘:
- 배경이 검정/짙은 회색으로 구워진 JPEG라, **테두리에서 연결된 배경만** flood fill로 지웠다.
  전역 임계값으로 지우면 그림 안의 어두운 윤곽선까지 뚫린다
- 제목 팻말 원본 오른쪽 아래에 **반짝이 표식(✦)** 이 찍혀 있어, 그냥 자르면 세로가 두 배가
  됐다. 가장 큰 덩어리 하나만 남겨 해결했다(잎은 팻말에 붙어 있어 같은 덩어리다)

### 검증

로비 실측 3뷰포트 × 7단계 문제 0건, 콘솔 에러 없음. 게임 방법 창은 1440·390에서 실제로
열어 확인했다(판이 화면 안, 팻말이 윗변에 정확히 절반씩, 바깥 스크롤 없음).
서버 테스트 63/63, 서버·클라이언트 빌드, 루트 `tsc` 통과.

## 17. 판 크기 고정 · 게임 방법 창 다듬기 (2026-09-08 밤, 2차)

### 판 크기를 못박았다 — 펼쳐도 1px도 안 움직인다

§16에서 `max-height: 100%`로 "스테이지를 넘지 않게"만 했는데, 그러면 규칙을 펼칠 때 카드가
405 → 558px로 **눈에 띄게 자란다.** 요청대로 아예 높이를 고정해, 펼치면 스크롤바만 생기게 했다.

> ⚠️ **`height: 100%`로는 안 됐다.** 액자(`::before`)는 카드 상자 **밖으로**
> `--board-mt`/`--board-mb`만큼 더 그려진다(원본 PNG의 투명 여백을 보정하느라). 카드가
> 여백을 꽉 채우니 그 몫이 넘쳐 `.stage-form`에 스크롤바가 생겼다 —
> **실측 scrollHeight 600 vs clientHeight 590, 정확히 그 차이였다.**
> 그래서 `calc(100% - var(--board-mt) - var(--board-mb))`로 미리 빼둔다. 두 값 모두
> `--board-edge`에 비례하므로 액자 두께를 바꿔도 따라온다.

이 값을 눈으로는 못 찾았다. 실측이 "폼스크롤 YES"를 낸 뒤 `.stage-form`/`.lobby-form-wrap`/
`.lobby-form-board`의 client·offset·rect·zoom을 직접 찍어보고서야 액자 오버행이 범인이라는
것이 드러났다. **이런 종류는 추측하지 말고 세 상자를 한 번에 찍어볼 것.**

실측이 전부 한 값으로 모였다 — 502(1280) / 617(1920) / 517(390). 여섯 칸이 모두 같다는 것이
"판이 고정됐다"는 증거다.

### 게임 방법 창

- **팻말에 세 줄을 새겼다** — HOW TO PLAY / 카드 뽑기 → 행동 하기 / 이 두 번이면 끝.
  판 안 첫 줄에 두면 팻말 바로 아래라 머리말이 두 개인 꼴이었다. `.howto-title-text`가
  판자의 **안쪽 홈**(폭 74% / 높이 62%)에만 앉는다 — 넘으면 마구리와 잎에 글자가 묻힌다
- **"이해했어요!"를 `PlankButton`으로** 바꿔 로비·대기실의 다른 버튼들과 맞췄다
- **귀퉁이 넷을 다시 잘랐다** — 아래 참고
- 귀퉁이 크기 1.2배(`--howto-orn` 132 → 190px, 모바일 84 → 121px)

> ⚠️ **귀퉁이를 각자 내용 경계상자로 바짝 자르면 위쪽 둘의 높이가 어긋난다.** 조각마다
> 크기가 달라지고 원본에서의 상대 위치가 사라지기 때문이다. 넷을 **모서리 기준 오프셋을
> 보존한 공유 캔버스**(360×266)로 다시 뽑아 해결했다 — `background-size` 하나로 넷이 함께
> 움직인다. 자세한 방법은 [`client/public/ui/README.md`](client/public/ui/README.md).
>
> 캔버스에 빈 여백이 함께 들어 있어 **같은 폭이라도 잎이 예전보다 작게 보인다.**
> 190px은 그 몫(360/299)에 1.2배를 곱한 값이다.

### 검증

로비 실측 3뷰포트 × 7단계 **문제 0건**(`.stage-form` 넘침 0). 방 만들기 접힘/펼침에서 판
크기가 완전히 같음을 1440·390에서 실제로 재 확인했다(484×593 → 484×593, 358×538 → 358×538,
스크롤 플래그만 false → true). 게임 방법 창도 두 화면에서 열어 확인했다.
서버 테스트 63/63, 서버·클라이언트 빌드, 루트 `tsc` 통과.

## 18. 작업 기록

- `NEXT_SESSION.md` — 새 환경에서 이어받기 위한 환경 구성·현황·다음 할 일
- `LOBBY_REDESIGN.md` — 설계안 v1/v2, 애니메이션 원칙, 실측 결과, 시행착오
- `client/public/ui/README.md` — 백보드·귀퉁이 장식·UI 아이콘 교체 절차와 실측값
- `ASSET_IDEAS.md` — 앞으로 넣으면 좋을 이미지 아이디어
- `오늘의_명령어_2026-09-08.txt` — 그림 버튼 3종, 기본 글꼴, 스크롤바, Render 배포
- `render.yaml` — Render Blueprint (대시보드에서 한 번 연결하면 push마다 자동 배포)
- `오늘의_명령어_2026-09-07.txt` — 결과 화면 2열, UI 아이콘 5종
- `오늘의_명령어_2026-09-06.txt` — upstream 2차 병합, 대기실·결과 창 액자, 메달·월계수, 폼 5:5
- `오늘의_명령어_2026-09-04.txt` — 폼 백보드 도입~다듬기
- `오늘의_명령어_2026-09-03.txt` — 패널 아트·글로우·서버 빌드 수정
- `오늘의_명령어_2026-09-02.txt` — 로비 개편
- `오늘의_명령어_2026-08-30.txt`, `request_history.txt` — 그 이전 기록

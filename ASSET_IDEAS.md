# 에셋 아이디어 — 로비 / 점수판 / 방 설정 창

**어떤 그림이 있으면 게임다워질지**를 정리한 문서다. 확정 계획이 아니라 **주문 목록의 초안**이고,
"무엇이 부족한가 → 무엇을 그려야 하는가 → 어떤 규격으로 받아야 하는가" 순서로 되어 있다.

> 실제로 넣을 때는 [`client/public/ui/README.md`](client/public/ui/README.md)의 교체 절차와
> [`NEXT_SESSION.md`](NEXT_SESSION.md) §2-6의 실측 기준값을 반드시 지킬 것. 이 프로젝트에서
> 로비 폼은 **한 줄만 늘어도 스크롤바가 생긴다.**

---

## 0. 지금 무엇이 문제인가 — 화면 3개를 실제로 띄워 보고 적음

로비는 완성도가 높은데 **그 뒤 화면들이 따라오지 못한다.** 사용자가 지나가는 순서대로 보면
품질이 계단처럼 떨어진다.

| 화면 | 지금 상태 | 한 줄 진단 |
| --- | --- | --- |
| 로비 첫 화면 | 카드테이블 일러스트 + 아트 패널 4종 + 나무 액자 폼 | **완성** |
| ~~대기실~~ | ~~흰 종이 한 장~~ → **나무 액자를 둘렀다(2026-09-05)** | 그릇은 해결. 안쪽 장식은 §1에 남음 |
| ~~결과 창~~ | ~~흰 카드 2장~~ → **나무 액자를 둘렀다(2026-09-05)** | 해결 |
| **게임 화면(플레이 중)** | 평평한 연두 바탕(`bg-jungle-50`) + 흰 카드들 | **남은 것 중 가장 큼** |
| 장소 타일 4종 | 오두막·부둣가·강가·숲길 일러스트 | **완성** |
| 행동 선택 카드 4종 | 동물 아트 + 설명 | **완성** |

> 2026-09-05에 **새 그림 없이** 로비 폼의 액자(`back_board.png` + 귀퉁이 장식)를 대기실과
> 결과 창에 재사용했다(`components/ui/BoardFrame.tsx`). 아래 §1의 "대안"이 실제로 통했다는
> 뜻이고, 그래서 §1의 그림 목록은 **필수가 아니라 "더 잘하려면"** 으로 내려간다.

즉 **"칸 안에 들어가는 그림"은 거의 다 있고, "칸 자체를 담는 그릇"이 없다.** 그래서 아래
제안은 대부분 배경·프레임·판때기 쪽에 몰려 있다.

### 이미 있는데 안 쓰는 것

- **`/ui/score_board.webp`** (1376×768, 47KB) — 나무 팻말이 달린 **가로형 백보드**.
  받아만 두고 아직 어디에도 쓰지 않는다(`lobbyAssets.ts`의 `scoreBoard`).
  아래 §2의 점수판이 정확히 이 그림을 위한 자리다. **새로 그리기 전에 이것부터 써 볼 것.**

---

## 1. 대기실 (`WaitingRoom`) — 우선순위 **상**

지금 대기실은 `bg-white rounded-2xl` 한 장이다. 배경 일러스트가 좌우로 보이는데 가운데만
하얗게 뚫려 있어, **로비에서 방 만들기를 누른 순간 세계관이 끊긴다.**

### 필요한 그림

| # | 이름(가칭) | 무엇 | 왜 |
| --- | --- | --- | --- |
| 1-1 | `board_wide.png` | **세로로 긴 나무 게시판** (9분할용) | 대기실 전체를 담는 그릇. 폼 액자(`back_board`)와 같은 나무·양피지 톤이되 세로로 길다 |
| 1-2 | `plate_roomcode.webp` | 방 코드를 얹는 **나무 팻말/명패** | 지금 방 코드가 그냥 큰 초록 글씨다. 못이나 끈으로 매단 팻말에 새기면 "이 방의 문패"가 된다 |
| 1-3 | `seat_slot_a/b.webp` | 팀별 **자리 매트** (좌식 방석·그루터기 느낌) | 참가자 목록이 회색 상자다. 팀 색(연두/파랑)이 밴 방석 위에 이름이 놓이면 "앉아 있다"가 보인다 |
| 1-4 | `seat_slot_spectator.webp` | 관전석 매트 (보라 계열, 살짝 뒤로 물러난 느낌) | 관전자가 팀과 **다른 종류의 자리**임을 색이 아니라 형태로 보여준다 |
| 1-5 | `chat_scroll.webp` | 채팅창 **양피지 두루마리** 바탕 | 채팅이 흰 상자다. 두루마리면 "대기실에서 나누는 이야기"로 읽힌다 |

### 대안 — 그림 없이 절반만 해결하기

새 그림 없이도 **`back_board.png`을 그대로 재사용**해 대기실을 감쌀 수 있다. 이미 9분할
(`border-image`)로 어떤 크기에도 늘어나게 만들어 뒀기 때문이다. 다만 대기실은 폼보다 훨씬
길어서 양피지 가운데가 심하게 늘어난다 — **세로로 긴 전용 그림(1-1)이 나오면 그쪽이 낫다.**

> 지금은 `.lobby-safe[data-stage="waiting"]`에서 안전영역 제약만 풀어 "화면을 다 쓰게" 해둔
> 상태다. 그릇을 씌우는 것은 그 다음 단계다.

---

## 2. 게임 점수판 (`TeamPanel` / `TeamTotalPanel`) — 우선순위 **상**

좌우 팀 패널은 흰 카드에 동물 4줄(경험치 바 + Lv) + 아래 큰 체력 구슬이다. 체력 구슬은
CSS로 그린 것치고 잘 만들어졌으니 **구슬은 두고 그 주변을 꾸미는 쪽**을 권한다.

### 필요한 그림

| # | 이름(가칭) | 무엇 | 왜 |
| --- | --- | --- | --- |
| 2-1 | **`score_board.webp` (있음)** | 팀 패널을 담는 나무 판 | **이미 받아둔 것.** 가로형이라 좌우 패널에 세로로 쓰려면 방향 확인 필요 |
| 2-2 | `exp_bar_frame.webp` | 경험치 바의 **나무 홈통 + 마개** | 지금 회색 바 하나다. 홈통에 액체가 차오르는 그림이면 "쌓인다"가 보인다 |
| 2-3 | `level_badge_{1..5}.webp` | 레벨 배지 (도토리 1~5개, 잎사귀 등) | 지금 `Lv. 0` 텍스트다. 레벨이 오르는 게 숫자가 아니라 **모양**으로 보이면 성취감이 커진다 |
| 2-4 | `hp_orb_frame.webp` | 체력 구슬을 **감싸는 받침대/고리** | 구슬 자체는 CSS가 잘 그린다. 받침만 씌우면 "장치"처럼 보인다 |
| 2-5 | `team_crest_a/b.webp` | 팀 문장(紋章) 자리 — 기본 2종 + 관전자 1종 | 팀 이름이 무작위로 정해지므로 이름 옆에 걸 **중립적인 문장**이 필요하다 |

> ⚠️ **레벨 배지를 만든다면 상한을 먼저 정할 것.** 레벨은 `floor(exp/threshold)`라 이론상
> 무한히 오른다. 배지를 5단계까지만 그리고 그 이상은 "5단계 배지 + 숫자"로 처리하는 식의
> 규칙이 그림보다 먼저 있어야 한다.

---

## 3. 방 설정 창 (닉네임·게임 규칙) — 우선순위 **중**

이쪽은 **이미 나무 액자 + 네 귀퉁이 장식**이 들어가 있어 급하지 않다. 다만 액자 안쪽이
여전히 평범한 HTML 입력창이라, 액자만 화려하고 내용물은 사무적인 대비가 있다.

### 필요한 그림

| # | 이름(가칭) | 무엇 | 왜 |
| --- | --- | --- | --- |
| 3-1 | `input_slot.webp` | 입력창 바탕 — 양피지에 **파인 홈** 느낌 (9분할) | 흰 사각형 입력창을 대체. 액자 톤과 이어진다 |
| 3-2 | `dice_button.webp` | 무작위 이름 뽑기 버튼 — **나무 주사위** | 지금 🎲 이모지다. 이모지는 OS마다 모양이 달라 통일감을 깬다 |
| 3-3 | `seat_icon_{a,b,spectator}.webp` | 자리 선택 버튼의 아이콘 | 지금 🟢🔵👀 이모지. 위와 같은 이유 |
| 3-4 | `rule_icon_*.webp` | 규칙 항목 아이콘 (목표 점수·축제·제한시간) | 지금 ⚙️🎯🌰⏳ 이모지. 7항목이 전부 텍스트라 훑어보기 어렵다 |
| 3-5 | `corner_tr/br` **교체본** | 우상·우하 귀퉁이 장식 | 좌측 둘만 새 그림으로 갈렸고 **우측 둘은 이전 스타일 그대로**다(`HANDOVER.md` §7) |

> **3-5는 그림 요청이 아니라 미완 정리다.** 네 귀퉁이 중 둘만 스타일이 달라 지금도
> 자세히 보면 어긋나 있다. 다른 무엇보다 먼저 맞추는 편이 싸게 먹힌다.

### 이모지를 그림으로 바꿀 때 주의

이 프로젝트는 이모지를 꽤 많이 쓴다(자리 배지, 규칙 라벨, 채팅 시스템 메시지, 팀 표시).
**전부 바꾸려 하지 말 것** — 대기실·채팅처럼 글 흐름 안에 섞인 이모지는 그림으로 바꾸면
줄 높이가 어긋나 §2-6 실측이 깨진다. **버튼·배지처럼 자리가 고정된 것만** 바꾼다.

---

## 4. 게임 화면 배경 — 우선순위 **중**, 그러나 효과는 가장 큼

게임 화면은 `bg-jungle-50` 단색이다. 로비가 카드테이블 위에서 벌어지는데 **정작 게임은
빈 방에서 하는 셈**이다. 배경 한 장이 §2의 다섯 항목보다 체감이 클 수 있다.

| # | 이름(가칭) | 무엇 | 왜 |
| --- | --- | --- | --- |
| 4-1 | `game_bg.webp` | 게임판 배경 — **로비 테이블의 위에서 본 버전** | 로비에서 앉았던 그 테이블에 카드가 깔리는 연결이 생긴다 |
| 4-2 | `game_bg_festival.webp` | 도토리 축제용 변주 (등불·도토리 흩날림) | 축제는 게임의 분기점인데 지금은 배지 하나로만 알린다. **배경이 바뀌면 사건이 된다** |
| 4-3 | `header_banner.webp` | 상단 턴 표시줄 바탕 — 나무 현판 | 지금 단색 진초록 띠다 |
| 4-4 | `commentary_scroll.webp` | 해설창 두루마리 (대기실 채팅 1-5와 같은 그림 재사용 가능) | 흰 상자 |

> **4-1은 로비 배경을 그대로 쓰면 안 된다.** 로비 배경은 인물이 사방을 둘러싼 구도라
> 게임 UI가 얼굴을 덮는다. 게임용은 **가운데가 비고 가장자리만 장식**인 구도여야 한다.

---

## 5. 규격 — 받을 때 이대로 요청할 것

### 5-1. 용량이 곧 첫 진입 대기시간이다 ⚠️

`public/`의 여섯 폴더(`places`·`skills`·`emoticon`·`howto`·`lobby`·`ui`)는 **전부 첫 화면에서
프리로드된다**(`generateAssetManifest.mjs`의 `IMAGE_DIRS`). 지금 합계는 이렇다.

| 폴더 | 용량 | 비고 |
| --- | --- | --- |
| howto | **10.2 MB** | `how_skills.png` 한 장이 **9.5 MB** |
| skills | **9.2 MB** | 4장이 각 2.1~2.6 MB |
| emoticon | 6.9 MB | 20장 |
| places | 2.0 MB | |
| lobby | 1.1 MB | 배경 12.9MB → 374KB로 줄인 결과 |
| ui | 0.6 MB | |
| **합계** | **약 30 MB** | |

**로비 배경을 12.9MB → 374KB로 줄여둔 노력이, 그 옆의 9.5MB PNG 한 장에 통째로 상쇄되고
있다.** 새 에셋을 더하기 전에 이 둘(`howto`·`skills`)을 WebP로 줄이는 것이 **어떤 새 그림보다
체감 개선이 크다** — 30MB → 5MB 아래로 내려갈 여지가 있다.

> 새 에셋의 예산: **한 장 200KB 이하, 전체 추가분 2MB 이하**를 권한다.
> 배경처럼 큰 그림은 WebP q80 전후, 아이콘·배지는 128~256px WebP면 충분하다.

### 5-2. 9분할(늘어나는 프레임)로 쓸 그림

`back_board.png`이 겪은 함정이 그대로 재현되므로 미리 정해서 요청한다.

- **바깥 투명 여백을 네 변 균등하게** 달라고 요청할 것 (지금 `back_board`는 상 78 / 하 67 /
  좌 57 / 우 56px로 제각각이라 CSS에서 `--board-mt/mr/mb/ml` 네 값으로 보정하고 있다)
- **장식(잎·버섯·도토리)을 프레임에 합쳐 넣지 말 것.** 9분할의 모서리 조각은 언제나
  `border-width` × `border-width`로 그려져 장식이 초록 얼룩으로 압축된다. 장식은 **따로**
- 가운데(양피지)는 **무늬가 옅고 균일하게** — 슬라이스 경계에서 톤이 어긋나면 이음매가 보인다

### 5-3. 배경으로 깔릴 그림

- `background-size: cover`에 잘리므로 **가운데를 비우고 가장자리에 주인공**을 두는 구도
- 그 위에 흰 글자가 얹히면 대비가 필요하다 — 너무 밝은 영역이 넓지 않게

### 5-4. 파일·경로 규칙

- 경로는 **반드시 `client/lib/lobbyAssets.ts` 한 곳**에만 적는다(CSS에 URL을 직접 쓰지 않는다)
- 새 폴더를 만들면 `generateAssetManifest.mjs`의 `IMAGE_DIRS`에 이름을 더한다 — 빠뜨리면
  로딩 화면이 끝난 **뒤에야** 받아와 그림이 늦게 뜬다
- 화면 공통 프레임은 `public/ui/`, 로비 전용은 `public/lobby/`

---

## 6. 권하는 순서

| 순서 | 무엇 | 이유 |
| --- | --- | --- |
| ~~0~~ | ~~대기실·결과 창에 액자 두르기~~ | **완료(2026-09-05).** 새 그림 없이 기존 액자 재사용 |
| 1 | **`howto`·`skills` PNG 용량 줄이기** | 그림 한 장 안 그리고 첫 진입이 크게 빨라진다. 30MB → 5MB |
| 2 | **`corner_tr`/`corner_br` 교체** (3-5) | 이미 어긋나 있는 미완 정리. 새 그림 2장 |
| 3 | **점수판에 `score_board.webp` 적용** (2-1) | 이미 있는 그림. 새로 그릴 필요 없음 |
| 4 | **게임 배경** (4-1) | 한 장으로 체감이 가장 크게 바뀜 |
| 5 | 레벨 배지·경험치 홈통 (2-2, 2-3) | 있으면 좋지만 없어도 게임은 읽힌다 |
| 6 | 대기실 전용 그림 (1-1 ~ 1-5) | 액자 재사용으로 급하지 않게 됐다 |

**1·2·3은 새 그림이 거의 필요 없다** — 이미 있는 것을 쓰거나 줄이는 일이다.
그림 주문이 필요한 것은 4(게임 배경)부터다.

> **액자를 새 화면에 두를 때는 `BoardFrame`을 쓴다.** 규칙은 `globals.css`의
> `.lobby-form-board, .board-frame` 공유 선언 한 곳에 있고, 안쪽 칸은 `.board-panel`,
> 글자는 `text-board-ink`/`text-board-muted`를 쓴다. `--orn-size`를 폼의 126px로 올리지
> 말 것 — 그 값은 `zoom: 1.75`가 걸린 폼 전용이고, 키가 낮은 카드에서는 위아래 장식이
> 서로 닿는다(HANDOVER.md §9).

---

## 7. 반영할 때 반드시 지킬 것

- 로비 폼에 무엇이든 더하면 **`client/scripts/measureLobby.mjs`로 다시 잰다.** 3뷰포트 × 7단계가
  전부 `no`여야 하고 값은 `NEXT_SESSION.md` §2-6과 대조한다
- **안내·경고는 새 줄로 덧붙이지 말고 `FormCard`의 설명 줄을 갈아끼운다** — 한 줄만 늘어도
  390×844에서 스크롤바가 생긴다(팀 이름 충돌·관전자 안내 모두 이 방식이다)
- 배경을 다른 구도로 교체하면 `globals.css`의 `--table-w/h`와 `--safe-*` 값을 다시 맞춘다
- 프레임 그림을 교체하면 **투명 여백 네 값을 다시 재야 한다**(`ui/README.md`에 재는 법)

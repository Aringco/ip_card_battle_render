# 플레이 화면 UI 입히기 — 로드맵

> 로비는 `LOBBY_REDESIGN.md`로 끝냈습니다. 이 문서는 **실제 게임 플레이 화면**을
> 같은 세계관(나무 액자 + 양피지 + 덩굴)으로 입히는 작업의 계획과 결과입니다.
>
> 작성 2026-09-11 · 상태 **P0~P4 완료 / P5·P6 남음**

---

## 0. 한 줄 요약

플레이 화면은 **지금 기능이 다 되어 있고 배치도 확정된 화면**입니다. 그래서 이 작업은
새로 만드는 일이 아니라 **가죽만 갈아입히는 일(skin pass)** 입니다 — 배치·컴포넌트·
애니메이션은 한 줄도 건드리지 않고, 각 상자에 `border-image` 기반 클래스를 **덧붙이기만**
합니다.

---

## 1. 지켜야 할 것 (요청받은 제약 그대로)

| # | 제약 | 코드에서 이게 뜻하는 것 |
| --- | --- | --- |
| 1 | **기존 이미지를 없애거나 형태를 바꾸지 않는다** — 기술 컷신, 캐릭터 얼굴, 표정, 감정표현 | `/skills/*_skill.png`, `/emoticon/*.png`, `/places/*.png`, `/ui/hand_*.webp`를 **참조하는 코드에 손대지 않는다.** 액자는 그 이미지 **바깥**에 두른다 |
| 2 | **레이아웃 배치를 바꾸지 않는다** | `GameLayout`의 3열 그리드(`19rem 1fr 19rem` × `1.25fr auto 1fr`)와 각 `gridColumn/gridRow`는 고정. 새 클래스는 **기존 요소에 추가**하고, 새 래퍼 `div`는 넣지 않는다(넣으면 그리드 자식이 바뀐다) |
| 3 | **글씨가 들어가는 에셋은 가로 3분할** (좌 마구리 │ 늘어나는 가운데 │ 우 마구리) | `border-width: 0 Npx` + `border-image: url(...) 0 S fill stretch` |
| 4 | **상자·이미지 영역은 9분할** (네 모서리 유지, 상하·좌우 가운데만 늘림) | `border-width: Npx` + `border-image: url(...) S fill stretch` |
| 5 | **액자가 바깥, 그림이 안쪽** — 그림이 액자 밖으로 나오면 안 된다 | 그림 레이어에 `inset: 0`이 아니라 `inset: var(--frame-inset)`을 준다(`.play-frame-inset`). 액자 클래스가 자기 두께에 맞춰 그 값을 정한다 |

### 1-1. 3분할과 9분할, 어느 쪽을 고르는가

로비에서 이 갈림길로 **같은 그림을 세 번 다시 만들었습니다**(`오늘의_명령어_2026-09-10.txt`
[1]~[3]). 판단 기준은 하나입니다 — **테두리 두께가 요소 크기를 따라가야 하는가?**

* **따라가야 한다 → 가로 3분할.** 위·아래 나무 레일을 *가운데 조각이 세로로 늘어나며*
  그리므로 두께가 높이에 비례합니다. 글씨 한 줄짜리 팻말·알약·게이지가 여기 해당합니다.
* **고정이어야 한다 → 9분할.** `border-image`의 **모서리 조각은 언제나
  `border-width` × `border-width`로 그려집니다.** 액자처럼 "판이 커져도 나무 두께는
  그대로"여야 하는 것이 여기 해당합니다.

> ⚠️ **슬라이스는 모서리 반지름 이상**이어야 합니다. 작으면 곡선의 절반이 변 조각에
> 섞여 늘어나며 뭉개집니다. 반대로 필요 이상 크게 잡으면 가운데 무늬가 변 조각에서
> 압축되어 **이음매**가 보입니다(`back_board`에서 겪음).
>
> ⚠️ **귀퉁이 장식(덩굴·버섯)은 액자 그림에 합치지 않습니다.** 합치면 모서리 조각
> 크기로 통째 압축돼 초록 얼룩이 됩니다. 별도 배경 레이어로 **고정 크기**로 얹습니다.
> 그리고 각 조각은 **자기가 붙을 모서리에 내용이 닿아 있어야** 합니다(2026-09-10 교훈).

---

## 2. 지금 화면 → 가고 싶은 화면

가안(두 번째 첨부)은 **똑같이 만들 대상이 아니라 방향**입니다. 지금 화면과 가안을
나란히 놓고 "무엇이 달라져야 하는가"만 뽑으면 다음 여덟 자리입니다.

| 자리 | 지금 | 가안이 말하는 것 |
| --- | --- | --- |
| 상단 헤더 | 짙은 초록 띠(`bg-jungle-800`) | **긴 나무 들보** + 왼쪽 끝 **양피지 이름표** |
| 단계 표시(장소/행동 선택) | 초록 알약 | 나무·초록·노랑 **알약 3종** |
| 축제 배지 | 주황 알약 | **도토리 두 알이 달린 짙은 나무 바** |
| 팀 패널(좌/우) | 연두/분홍 둥근 상자 | **줄이 그어진 노트 카드**(초록/분홍), 각 줄이 알약 |
| 카드판 가운데 | 흰 반투명 상자 | **양피지 판**(네 귀퉁이 덩굴) |
| 장소 타일 4칸 | 사진 + 둥근 모서리 | 사진 그대로, **나무 액자를 두름** |
| 해설판 + 타이머 | 흰 상자 + 파란 막대 | 양피지 띠 + **모래시계가 달린 게이지** |
| 체력판(하단 좌/우) | 유리구슬 | **초록/분홍 노트 카드**에 숫자 |
| 행동 선택 5칸 | 컷신 + 검은 배경 | 컷신 **그대로**, 나무 액자를 두름. [턴 마치기]는 **초록 화살표** |

**바꾸지 않는 것** — 장소 사진, 기술 컷신, 동물 얼굴·표정, 손가락 가이드, 모든 연출
레이어(`components/effects/`), 그리드 배치, 클릭 판정.

---

## 3. 에셋 시트 → 조각 대응표

첨부된 시트(2560×1376, 투명 배경) 한 장에서 아래 조각들을 잘라 씁니다. **필요 없는
것은 넘어갑니다** — 시트에는 이 화면에 쓸 자리가 없는 조각(자물쇠·나침반·왕관 배지·
말풍선 등)이 함께 들어 있습니다.

| 잘라낼 조각 | 파일명(예정) | 분할 | 쓰는 곳 |
| --- | --- | --- | --- |
| 긴 나무 들보 | `play/beam.webp` | **3분할** | 상단 헤더 바닥 |
| 잎 달린 작은 양피지 이름표 | `play/nameplate.webp` | **3분할** | 헤더 왼쪽 "○○ 차례" |
| 초록 알약 | `play/pill_green.webp` | **3분할** | 단계 표시(활성), 플레이어 이름(현재 차례) |
| 노란 알약 | `play/pill_gold.webp` | **3분할** | 단계 표시(다음 단계 예고) |
| 짙은 나무 알약 | `play/pill_wood.webp` | **3분할** | 단계 표시(비활성), CPU 이름표 |
| 도토리 두 알 달린 짙은 바 | `play/acorn_bar.webp` | **3분할** | 축제 진행 배지 · 축제 뽑기 진행도 |
| 모래시계 게이지(틀) | `play/gauge_frame.webp` | **3분할** | 턴 타이머 바깥 틀 |
| 게이지 채움(초록 캡슐) | `play/gauge_fill.webp` | **3분할** | 턴 타이머 안쪽 채움 |
| 나무 액자 + 양피지(가로형 큰 판) | `play/board_h.webp` | **9분할** | 카드판 가운데, 해설판 |
| 나무 액자 + 양피지(세로형) | `play/board_v.webp` | **9분할** | 장소 타일 액자 |
| 줄 노트(초록) | `play/note_green.webp` | **9분할** | 우리 팀 패널 · 우리 체력판 |
| 줄 노트(분홍) | `play/note_pink.webp` | **9분할** | 상대 팀 패널 · 상대 체력판 |
| 초록 화살표 | `play/arrow_green.webp` | 고정 크기 | [턴 마치기] 칸 |
| 버섯·풀·돌·도토리 | `play/decor_*.webp` | 고정 크기 | 화면 네 귀퉁이 |

> ⚠️ **자를 때 지킬 것** — ① 조각마다 **투명 여백을 남기지 않는다**(슬라이스는 그림
> 가장자리부터 잰다). ② 3분할용 조각은 **원본 해상도 그대로** 쓴다(줄이면 두께가
> 고정된다 — 2026-09-10 [3]의 실수). ③ 잘라낸 뒤 `alpha > 8` 경계상자를 찍어
> 의도한 크기인지 확인한다.

---

## 4. 컴포넌트별 적용 계획

새 클래스는 전부 `client/app/globals.css`의 **"플레이 화면 — 나무 UI"** 절에 모읍니다.
경로는 `client/lib/playAssets.ts` 한 곳에서만 참조합니다(로비의 `lobbyAssets.ts`와 같은 원칙).

| 컴포넌트 | 붙일 클래스 | 에셋 | 주의 |
| --- | --- | --- | --- |
| `GameHeader` `<header>` | `.beam-bar` | beam | 높이 `min-h-[3.25rem]` 유지. 글자색을 나무 위에서 읽히는 크림색으로 |
| `GameHeader` 왼쪽 차례 표시 | `.name-plate` | nameplate | 닉네임이 길면 늘어나야 하므로 3분할 |
| `StepPill` | `.pill-chip` + `-active/-inactive` | pill_green / pill_wood | **기존 `.step-pill-*`를 덮어쓰지 말고 새 클래스로 교체** — 전환 애니메이션이 붙어 있다 |
| 축제 배지 | `.acorn-bar` | acorn_bar | 깜빡임은 `filter: opacity()`로(같은 속성 충돌 회피 — 로비 교훈) |
| `TeamPanel` 바깥 상자 | `.note-card` + 팀 톤 | note_green/pink | **`bgClass`(연두/분홍)를 지우지 말 것** — 관전 시점(`spectator-team-panel`)이 이 분기를 탄다 |
| `PlayerList` 각 줄 | `.pill-chip` | pill_green / pill_wood | 현재 차례 줄의 `glow-pulse` 유지 |
| `ScorePanel` 동물 행 | `.row-plate` | note 안쪽 줄로 대체 가능 | 얼굴 이미지·레벨 배지·경험치 막대는 그대로 |
| `GameBoard` 바깥 | `.board-frame-play` | board_h | `border-2`를 액자로 바꿔도 **`board-my-turn` 맥동 테두리는 살려야 한다** → 맥동은 `outline`이나 `box-shadow`로 옮긴다 |
| `AnimalStackArea` 4줄 | `.parchment-row` | board_h 안쪽 | 카드 겹침 계산(`CARD_WIDTH_PX`)에 영향 주지 않도록 padding만 |
| `PlaceTile` | `.wood-card-frame` | board_v | 사진(`place-tile`)은 액자 **안쪽**에. `overflow-hidden`이 손가락 가이드를 자르지 않도록 래퍼 구조 유지 |
| `CommentaryBoard` | `.board-strip` | board_h | 높이 `h-20` 고정 유지 |
| `ActionPrompt` 흰 알약 | `.parchment-pill` | board_h(작게) 또는 nameplate | `pointer-events-none` 유지 |
| `TurnTimer` | `.gauge-frame` / `.gauge-fill` | gauge_* | ⏳ 이모지는 그림에 이미 있으면 **HTML 이모지를 지운다**(두 번 보인다) |
| `SkillChoiceBar` 각 칸 | `.wood-card-frame` | board_v | 컷신 `background-image`와 `skill-choice-dim`은 그대로. 액자는 **버튼 바깥 래퍼**에 |
| [턴 마치기] 칸 | `.pass-panel-bg`에 arrow | arrow_green | 지금도 화살표 배경이 있다 — 그림만 교체 |
| `TeamTotalPanel` | `.note-card` | note_green/pink | ⚠️ **구슬 연출(`hp-orb-liquid`/`crack`/`shard`)을 지우지 말 것.** 노트 카드를 **액자로** 두르고 구슬은 그 안에 둔다 |
| `SheepProgressBar` / `FestivalProgressBar` | `.gauge-frame` | gauge_* / acorn_bar | 진행도 계산 그대로 |
| 화면 네 귀퉁이 | `LeafDecoration` 유지 + `.play-decor` | decor_* | 지금 잎 장식은 그대로 두고 버섯·돌만 추가 |

---

## 5. 더 필요한 에셋 (tmp 처리)

> **tmp 처리 방식** — 없는 에셋 자리는 **가짜 그림을 그려 넣지 않습니다.** 로비에서
> placeholder 표식(점선 테두리·해치 무늬)을 깔았다가 "웬 점선 사각형"으로 보여
> 되돌린 적이 있습니다. 대신 **지금 모습을 그대로 두고** 이 표에 적어 둡니다.

시트를 받아 실제로 잘라 보니 처음 적었던 목록 중 셋(T1·T6·T7)은 시트 안에 있었습니다.
남은 것과, 작업하며 새로 드러난 것을 함께 적습니다.

| # | 필요한 것 | 왜 필요한가 | 지금은 | 판단 |
| --- | --- | --- | --- | --- |
| ~~T1~~ | ~~세로 나무 액자~~ | — | `board_v`로 해결 (장소 타일에 적용) | ✅ |
| T2 | **게이지 채움 3색**(파랑·주황·빨강) | 타이머가 10초·5초에서 색이 바뀐다 | 초록 한 벌에 `hue-rotate` | 쓸 만함. 정색이 오면 교체 |
| T3 | **좌우 대칭 이름표·노트** | 오른쪽 팀은 뒤집힌 구도가 자연스럽다 | `scaleX(-1)` 가능 | 낮음 |
| T4 | **상단 캐릭터 일러스트 4종**(양·토끼·인어·호랑이 상반신) | 가안 위 모서리에 걸터앉아 있다. **시트에 없다** | 넣지 않음 | 있으면 가안에 가장 가까워짐 |
| T5 | "내 팀" 별 배지 | 지금은 흰 알약 + ⭐ | 현행 유지 | 낮음 |
| ~~T6~~ | ~~자물쇠 금지 마크~~ | — | `icon_lock` 확보 (아직 미적용, P2 잔여) | ✅ |
| ~~T7~~ | ~~가로로 긴 양피지~~ | — | `board_h`를 3분할해 해결 | ✅ |
| **T8** | **헤더용 2단 이름표**, 또는 헤더 높이를 늘려도 좋다는 결정 | 헤더가 42px이라 양피지가 세로로 눌려, 가안처럼 "팀 이름 / 닉네임 차례" 두 줄을 넣을 수 없다 | 한 줄로 눌러 담음 | **결정 필요** |
| ~~T9~~ | ~~행동 선택 5칸 액자~~ | — | **금색 액자 시트로 해결.** 나무 액자와 달리 막대가 얇아(8px) 12px 안여백 안에 들어간다 — 글자를 밀어낼 일이 없었다 | ✅ |
| **T10** | 팀 패널·체력판도 같은 사정 | 노트 카드 액자를 두르면 동물 표가 그만큼 좁아진다 | 액자 없음 | **결정 필요** |

> T9·T10은 에셋 문제가 아니라 **"제약 2를 어디까지로 볼 것인가"** 의 문제입니다.
> 바깥 상자 위치·크기는 그대로지만 **안쪽 내용이 20px씩 안으로 밀립니다.**
> 허용된다면 P4·P5를 그대로 진행하고, 아니라면 그 두 자리는 액자 없이 남깁니다.

## 6. 진행 순서

작업을 한 번에 다 하지 않고 **위에서 아래로** 내려갑니다. 각 단계마다 스크린샷으로
"배치가 안 움직였는지"를 먼저 확인합니다 — 제약 2가 깨지면 그 뒤 단계가 전부 흔들립니다.

| 단계 | 범위 | 확인 방법 |
| --- | --- | --- |
| **P0** | 에셋 자르기 + `playAssets.ts` + CSS 슬롯 뼈대 | 조각별 경계상자 출력 |
| **P1** | 헤더 줄 — 들보·이름표·단계 알약·축제 바 | 1600×900 / 1280×800 스크린샷 대조 |
| **P2** | 카드판 — 가운데 양피지 + 장소 타일 액자 | 손가락 가이드가 안 잘리는지, 맥동 테두리가 남았는지 |
| **P3** | 해설판 + 타이머 게이지 | 타이머 색 3단계(평상/경고/위험) 모두 확인 |
| **P4** | 행동 선택 5칸 액자 | 컷신이 안 가려지는지, hover 확대가 안 잘리는지 |
| **P5** | 팀 패널 + 체력판 노트 카드 | 관전 시점(중립 2색)에서도 색이 맞는지 |
| **P6** | 귀퉁이 장식 · 다듬기 | 1920×1080 / 1366×768에서 넘침 없음 |

---

## 7. 시작 전에 알아야 할 함정

로비에서 이미 값을 치른 것들입니다. 플레이 화면에서 **똑같이 재현될 자리**를 적어 둡니다.

* **`overflow`는 바깥으로 그린 것도 잘라낸다.** `TeamPanel` 안쪽이 `overflow-y-auto`,
  `PlaceTile` 버튼과 기술 칸이 `overflow-hidden`입니다. 액자를 `::before`로 두르면
  그 자리에서 잘립니다 — 액자는 **잘리지 않는 바깥 래퍼**에 그립니다(손가락 가이드가
  이미 같은 이유로 바깥에 나와 있습니다).
* **`filter`는 자식 전체에 걸린다.** 장소 타일·기술 칸이 배경 레이어에만 `filter`를
  거는 이유입니다. 액자에 `filter`를 걸면 안쪽 글자까지 어두워집니다.
* **애니메이션은 같은 속성의 transition을 덮어쓴다.** 깜빡임은 `opacity`가 아니라
  `filter: opacity()`로.
* **`hp-orb`·`skill-choice-*`·`place-tile-*`에는 연출이 매달려 있다.** 클래스를
  **교체하지 말고 덧붙입니다**. 지우면 특허랑이 금·토끼 비행·눌림 연출이 조용히 죽습니다.
* **관전 시점(`myTeam === null`) 분기를 빠뜨리지 말 것.** 새로 팀 색을 쓰는 자리마다
  `spectatorTeamVars()`를 함께 넘겨야 합니다.

---

## 8. 구현 결과

### 8-1. P0 — 에셋 자르기 ✅

시트(2560×1376)는 **알파가 없는 3채널 PNG**로 도착했습니다 — 편집기의 체커보드가
픽셀로 구워져 있었습니다. `client/scripts/cutPlaySheet.mjs`가 ① 테두리에서 연결된
체커보드만 flood fill로 지워 투명도를 되살리고 ② **연결 성분 단위**로 골라 옆 조각이
딸려 오지 않게 한 뒤 ③ 경계상자에 바짝 잘라 **38조각 517KB**를 만듭니다.

* 경로는 `client/lib/playAssets.ts` 한 곳 · 분할 값은 `client/public/play/README.md`
* 들보는 시트에서 **왼쪽 끝이 이름표에 가려 마무리가 없어**, 오른쪽 덩굴 끝을
  좌우 반전해 붙여 양쪽이 같은 마무리가 되게 했습니다(2190×209).
* `public/play`를 `generateAssetManifest.mjs`의 `IMAGE_DIRS`에 넣어 프리로드에 실었습니다.

### 8-2. 배치가 안 움직였는지 재는 도구 ✅

`client/scripts/measurePlay.mjs` — 혼자 놀기로 실제 게임에 들어가 1920×1080 /
1600×900 / 1366×768에서 **장소 선택·행동 선택 두 단계**의 상자 10개를 잽니다.
`--save baseline`으로 기준을 잡고 `--diff baseline`으로 달라진 것만 봅니다.
**이 작업의 모든 커밋에서 "움직인 상자 없음"을 확인했습니다.**

### 8-3. P1 — 헤더 ✅

| 무엇을 | 어떤 에셋으로 | 함정 |
| --- | --- | --- |
| 헤더 바닥 | `beam` 3분할 | `shadow-md`를 뺐다 — 들보는 모양 있는 그림인데 box-shadow는 **상자의 네모**를 따라 그려져 투명한 자리에 그늘이 드러난다 |
| 왼쪽 "○○ 차례" | `nameplate` 3분할 | 위아래 안여백이 곧 양피지의 키다. 줄높이만큼만 두면 **글씨가 양피지 밖 나무틀 위로 걸린다**(실제로 걸려 9px로 늘렸다) |
| 단계 알약 | `pill_green` / `pill_wood` | `px-2.5`를 뺐다 — 그림의 마구리가 이미 그 몫이라, 남기면 알약이 20px 넓어져 헤더 가운데가 밀린다 |
| 축제 배지 | `acorn_bar` 3분할 | `.festival-header-badge`의 깜빡임은 `color`만 건드려 그대로 살아 있다 |

### 8-4. P2 — 장소 타일 ✅ (금지 마크는 남음)

네 타일에 `board_v` 액자를 **`fill` 없이** 둘렀습니다. `fill`을 쓰면 가운데 양피지가
장소 사진을 통째로 덮습니다. 없이 쓰면 여덟 조각(네 변·네 모서리)만 그려져 사진
가장자리에 액자만 얹힙니다 — **사진·라벨 이미지는 한 픽셀도 바뀌지 않았습니다.**

귀퉁이 잎이 눌리지 않는 이유는 **슬라이스가 정사각(90×90)이고 네 변 두께가 같아서**
입니다(90 → 28px, 가로·세로 같은 배율). 로비처럼 장식을 분리할 필요가 없었습니다.

남은 것 — 금지 마크를 `icon_lock`으로 교체(지금은 SVG 원+사선).

### 8-5. P3 — 해설판 · 안내 쪽지 · 타이머 ✅

* **해설판** → `board_h`를 **가로 3분할**. 9분할로 쓰면 위아래 테두리가 40px을 먹어
  세 줄이 안 들어갑니다. ⚠️ 처음에 `label_small`로 했다가 되돌렸습니다 — 그 그림은
  잎이 삐져나온 위아래 투명 여백이 커서, 64px로 눌러 담으면 양피지가 아래 3분의
  2에만 깔리고 **글자 첫 줄이 허공에 떴습니다.**
* **안내 쪽지**(ActionPrompt) → `label_small` 9분할. `shadow-sm`은 뺐습니다(위와 같은 이유).
* **타이머** → `gauge_track` + `gauge_fill` 3분할. 채움 그림이 초록 한 벌뿐이라
  경고·위험 단계는 `filter: hue-rotate()`로 만듭니다(⚠️ filter는 **채움 요소에만**).
  트랙에 `overflow-hidden`을 걸지 않습니다 — 채움이 자기 오른쪽 둥근 끝을 그리는데
  잘라내면 그 자리가 각지게 끊깁니다.

### 8-6. 남은 단계

| | 범위 | 막는 것 |
| --- | --- | --- |
| P2 잔여 | 장소 금지 마크 → `icon_lock` | 없음 |
| ~~P4~~ | ~~행동 선택 5칸 액자~~ | ✅ 완료 — 금색 액자 + 모서리 장식 |
| **P5** | 팀 패널 · 체력판 노트 카드 | **T10** — 동물 표가 20px씩 좁아짐 |
| P6 | 화면 네 귀퉁이 장식(`decor_*`) | 없음 |

⚠️ **P5에서 `hp-orb-*`를 지우지 말 것.** 특허랑이 금·파편·상표토끼 압박 연출이
전부 그 클래스에 매달려 있습니다. 노트 카드는 **액자로 덧두르고** 구슬은 그 안에 둡니다.

### 8-7. P4 — 기술 카드 금색 액자 ✅ (두 번째 시트)

카드 전용 시트(1920×1040)를 따로 받아 **10조각**을 더 잘랐습니다 — 금색 사각 액자,
네 모서리 장식, 잎 데칼 4종, 그리고 이름표 칸(요청대로 **보류**, 에셋만).

| 무엇을 | 어떻게 | 함정 |
| --- | --- | --- |
| 테두리 | **CSS 그라데이션**(5px · radius 10px) | 아래 "되돌림" 참고 |
| 네 모서리 장식 | 고정 크기 배경 4장(`::before`, 39px) | **테두리에 합치면** 모서리에서 눌려 얼룩이 된다 |

> **되돌림(2026-09-11)** — 처음에는 `card_frame.webp`를 9분할로 둘렀지만, 카드가
> 217px밖에 안 되는 자리에서 금색 막대가 뭉툭했고 **두께를 줄이면 모서리 곡선까지 함께
> 작아져** 조절할 여지가 없었다. CSS 그라데이션은 두께와 둥글기를 따로 정한다.
> 진짜 `border`라 `--frame-inset`도 0이면 된다 — 절대배치 자식의 기준 상자가 부모의
> **padding box**여서 `inset: 0`이 곧 테두리 안쪽이기 때문이다.
> 그림(`card_frame.webp`)은 되돌릴 수 있게 남겨 뒀다.

⚠️ **안이 막힌 액자는 가운데를 따로 뚫어야 합니다.** 금색 막대가 사방을 둘러싸고 있어
테두리에서 출발하는 flood fill이 안쪽에 닿지 못했고, 그대로 두면 액자 속이 체커보드로
꽉 찬 불투명 판이 되어 컷신을 가립니다(막대 두께를 재니 826px이 나와 알아챘습니다).
`cutPlaySheet.mjs`에 `hollow: true`를 더해 가운데에서 한 번 더 fill합니다.

모서리 장식은 40 → 26 → **39px**(요청받은 1.5배)로 오갔습니다. 39px에서는 우상단
"레벨 부족" 표시와 긴 효과 문구가 장식에 덮여, 그 줄을 `top-2` → `top-[42px]`로 내려
장식 **아래**에 앉혔습니다.

**T9가 이렇게 풀렸습니다** — 나무 액자였다면 글자 안여백을 늘려야 했지만, 금색 막대는
8px이라 기존 12px 안여백 안에 들어갑니다. 안쪽 배치를 한 픽셀도 바꾸지 않았습니다.

### 8-8. 작업하며 다시 확인한 것

* **dev 서버의 낡은 CSS를 세 번 만났습니다.** globals.css에 규칙을 덧붙일 때마다
  Turbopack이 청크를 다시 만들지 않아, 화면에는 직전 묶음까지만 반영됐습니다.
  **`rm -rf client/.next` 후 재시작**이 매번 해결책이었습니다 —
  CSS를 여러 번 고칠 때는 **한 번에 몰아서 고치고 마지막에 한 번 재시작**하는 편이 낫습니다.
* **JSX는 `return (` 바로 뒤에 주석만 둘 수 없습니다.** `{/* … */}`를 첫 줄에 넣었다가
  컴파일이 깨졌고, 실측 도구가 "게임 진입 실패"로 잡아냈습니다.
